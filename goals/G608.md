# G608 Security Permissions Audit

/goal RLS、Storage、signed URL、service role、secret redaction、admin-only surface、marker-scoped cleanup の read-only audit を行い、低リスクで修正可能な問題を潰す。秘密値を出力しない。認証情報入力や本番権限変更が必要なら exact blocker と human-needed にする。

## Done Evidence

- Audit command/output summary with secrets redacted.
- Findings fixed/deferred.
- Read-only DB/storage/API evidence where safe.
- Static checks and Codex review if code changed.

