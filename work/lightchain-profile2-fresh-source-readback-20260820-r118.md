# Lightchain fresh source readback r118

## result

Fresh current-selector, target-scoped, read-only source confirmation completed
for the Lightchain production homepage.

- Backend: `chrome_plugin`
- Profile/surface: `Profile 2` / `signed_chrome_extension_profile2`
- Selector revision: `30`
- Preflight: `status=ready`, `exact_blocker=null`
- Browser-client boundary: `29a71ee8-15c8-4e98-9e3c-7a4a2d37ac41`
- Browser id: `-e668-4ff8-93a4-b6257ed1c079`
- Same-run handshake: `list -> get -> openTabs`, `openTabs=2`
- Provisioned source target: tab `1980904692`
- URL/title: `https://jp.linkaigc.com/` / `Lightchain AI`
- Hydrated DOM readback: body length `2047`, visible controls `40`
- Cleanup: `cleanup_verified=true`; created tab `1980904692` closed

## source readback

The current homepage visibly exposed these category controls:

- `おすすめ` (`Hot`)
- `企画デザインツール`
- `AIフィッティング`
- `グラフィックツール`

The same DOM readback exposed the current Lightchain workspace copy and
homepage examples for fabric/material replacement, AI fitting, model/body/pose
changes, flat sketch plus fabric, multi-angle output, vector/graphic tools,
marketing, and design-agent workflows. The visible prompt input was
`指示を入力してください... 例：『モデルの着せ替え』`.

## boundary

This artifact is a fresh source homepage/readiness proof. It is not a provider
generation, save, Gallery/Canvas/History/Jobs, reuse, internal-user acceptance,
or foreground-operation proof. The official extension advertised `viewport` at
browser level and `pageAssets`/`cdp` at tab level; `foreground_activation` and
`management` were not advertised. No selected-tab, claim, focus, writer lease,
provider generation, save, download, rights confirmation, or external effect
was executed. The only tab mutation was the explicitly allowlisted,
task-owned source provisioning and its cleanup.

## next action

Keep the source homepage/category evidence as the current parity baseline. When
the official foreground capability is advertised, start a new Profile 2 owner
and prove fabric/printing generation through durable result/save/reuse first,
then AI fitting. Do not reuse this read-only binding, tab, run, or artifact for
foreground work.
