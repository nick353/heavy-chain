# Heavy Chain 10M Product Readiness Plan

Updated: 2026-07-03

## Goal

Heavy Chain を、Lightchain の上位互換として違和感なく使えるだけでなく、1000万ユーザー規模を狙える商品品質へ近づける。対象は UI/UX、実生成品質、衣服認識/切り抜き/レイヤー、Canvas、失敗時UX、オンボーディング、テンプレート、パフォーマンス、スケール、監視、セキュリティ、法務/安全、リテンション、βユーザー相当タスク、競合比較、品質基準、release-gate回帰、運用ドキュメント。

## Current Baseline

- Lightchain 4カテゴリ構成、生成ホーム、機能クリック後の詳細画面、素材アップロード反映、旧重複サイドバー排除は実装・本番確認済み。
- 全10主要生成機能は過去 proof で readback と目視確認済み。`STATE.md` には 2026-06-26 の全10機能fresh proof と後続prompt polish proofもあるため、G601 は無条件にcreditを消費する再実行ではなく、既存proofのfreshness/readback/画像品質を再監査し、欠けている機能または古い/弱い証跡だけを bounded fresh generation で補う。
- Runway は `localhost:15554` 動的クライアント経路ではなく、Codex承認済み Runway MCP と local worker handoff を正とする。
- 本番 launch-ops / production monitor は `goal-loop-10m-20260626` で Lightchain式UIへ追従済み。直近 proof は `output/playwright/goal-loop-10m-20260626/launch-ops-after-deploy/summary.json`、`production-monitor-after-deploy/summary.json`、`lightchain-clone-prod-after-deploy/SUMMARY.json`。

## Stop Conditions

- 課金、購入、支払い、checkout、本人確認、OTP/CAPTCHA、秘密情報入力、外部公開では停止する。
- 外部サービスの設定変更、新しい有料ベンダー、公開投稿、不可逆削除、RLS/storage/authの破壊的変更は停止し、必要な決定・証跡・リスクを残す。
- Runway workspace limit、認証切れ、生成失敗、Playwright/Chrome権限ブロック、Supabase権限ブロックが起きた場合は、exact blocker、URL/DOM/log、再開条件を残す。

## Evidence Requirements

- UI/UX: URL、DOM/body text、スクリーンショット、録画、desktop/mobile、console/page/network failures。
- 生成品質: prompt、feature、job/task ID、worker/readback、Storage/DB証跡、画像ファイル、目視scorecard。
- Canvas/レイヤー: 画像配置、選択状態、layer/mask/material metadata、export/readback。
- 性能/スケール: bundle/route timing、画像数負荷、DB/Storage readback、monitor summary。
- セキュリティ: RLS/storage/signed URL/service-role露出/secret redaction の read-only audit。
- 法務/安全: 利用規約/商用利用/著作権/ブランド模倣/ユーザー素材保存の decision packet。現時点で未決定なら `human-needed` とする。

## Execution Steps

1. [done] 全10主要生成機能の既存fresh proofを再監査し、不足分だけ bounded fresh generation で再検証し、画像品質scorecardを更新する。
2. [done] Lightchain同等UXの最終差分を、生成フローとCanvas連携を中心に再比較する。
3. [done] 衣服認識、切り抜き、レイヤー、素材メタデータ、Canvas編集導線を深掘り検証し、足りないUIを実装する。
4. [done] 生成失敗時UX、worker待ち、Runway制限、参照画像失敗のユーザー向け表示を検証・改善する。
5. [done] 初回オンボーディングとテンプレート体験を、10分以内に価値体験できる形へ磨く。
6. [done] パフォーマンス、画像一覧負荷、Canvas負荷、bundleを測定し、明確な改善を入れる。
7. [done] production monitor、launch-ops、mass-market QA、release-gate回帰を統合した gate を作る。
8. [done] セキュリティ/権限/RLS/storage/signed URL/service role の read-only audit を実行する。
9. [done] 法務/安全/商用利用/素材保持/ブランド模倣ガードの decision packet を作る。
10. [done] リテンション機能、ブランドキット、履歴検索、テンプレ再利用、チーム共有の現状差分を整理し、実装できる低リスク項目を入れる。
11. [done] βユーザー相当の実使用タスクを5シナリオ以上録画で流し、詰まりを修正する。
12. [done] 競合比較を Lightchain / Canva / Kittl / Photoroom / Adobe Express / Runway / Shopify系AI画像ツールで更新する。
13. [done] 画像品質基準、NG例、prompt preset、機能別rubricを docs と verifier に落とす。
14. [done] 運用ドキュメント、障害復旧、worker起動、handoff、rollback、monitor対応を最新化する。
15. [done] 最後にrelease gate範囲のreadback回帰、ドキュメント更新、Codex reviewを通す。
16. [done] 2026-06-27 continuation auditで、G603/G605/G610の深い操作をZeabur本番で再実行する。
17. [blocked-exact] 全10主要機能を「既存proof再監査」ではなく、過去資産流用なしの同一run fresh generationとして再実行する。2026-06-30T06:37Z の最新G617試行では、直前の最小 Runway MCP `gen-4` availability probe は完了画像URLを返したが、その直後の新規 run `hc-g617-same-run-fresh-20260630T063740Z` の1件目 `campaign-image` 本実行で `workspace_limit` が返ったため停止した。認証や `mcp-remote` consent ではなく、接続済みRunway workspace/modelの sustained generation availability が現在のexact blocker。
18. [done] 10M級と呼ぶためのローカル合成負荷 + production read-only ops baselineを追加する。G618で1200画像/600 Canvas object、Canvas PNG export実ファイル検査、96h production DB/Storage/usage/worker readback、release-gate接続を証跡化した。本番同時実行負荷試験や外部alerting導入は行っていない。
19. [queued] 実βユーザーまたは外部協力者による本番利用証跡を、個人情報と公開操作を避けて収集する。
20. [done] security auditをread-only/staticから、権限悪用ケース、監査ログ、事故対応runbook、運用監視の証跡へ拡張する。G620でabuse-case matrix、audit sources、incident response、96h production monitor SLO、release-gate接続を証跡化した。
21. [human-needed] H601/H602の法務、素材保持、ブランド/人物ポリシー、課金/外部公開判断をユーザー/operatorが決める。H601は完全closeしていないが、生成前の権利確認、第三者ブランド/人物likenessブロック、生成系Edge Function共通のサーバー側H601ガード、Terms/Privacy/Legal caveat、`npm run verify:h601-legal-safety`、release-gate接続までは実装済み。
22. [blocked-exact:prod-entrypoint] 2026-06-30の本番全機能QAではZeabur URLのアプリ導線は通ったが、独自ドメイン `https://heavy-chain.com` は `ERR_CONNECTION_REFUSED`。2026-07-01の再readbackでZeabur `/generate?feature=campaign-image` のH601権利確認ラベル、商用利用 caveat、チェックボックスは本番表示済みになった。public launch前に残るentrypoint blockerはDNS/hostingの復旧または正規公開URLの決定。
23. [done-prod:lightchain-beginner-ux] 2026-07-01に `/lightchain/:toolId` 詳細画面を初心者向けにシンプル化した。スクショ確認後、初期画面はさらに削り、アップロード前は素材入力と折りたたみ詳細だけにした。`AIマスク認識`、詳細設定、レイヤー詳細、プレビュー、参考条件、Canvas保存は画像アップロード後に表示される。アップロード後の主導線は upload -> AIマスク認識 -> 抽出して次へ -> Canvas保存。ローカル証跡は `output/playwright/lightchain-beginner-ux-20260701-r4/SUMMARY.json`、`output/playwright/lightchain-mask-layer-flow-20260701-simple-r7/SUMMARY.json`、`output/playwright/g603-garment-layer-canvas-20260701-simple-r2/SUMMARY.json`、`output/playwright/lightchain-all-feature-workflows-20260701-simple-r7/SUMMARY.json` すべて `ok=true`。本番証跡は `output/playwright/prod-post-deploy-lightchain-20260701-r3/SUMMARY.json` `ok=true` で、保存済み認証のZeabur詳細routeがpublic landingではなく、アップロード前にAIマスク/Canvas保存/レイヤー詳細/参考条件を隠すことを確認済み。

## Completion Criteria

- 全10主要機能の fresh generation は既存proof再監査ではなく、同一runの過去資産流用なし proof で完了、または exact blocker 付きで分割再開可能になっている。
- 生成画像の scorecard が全機能で `pass`、または `needs-polish` の具体的修正が反映済み。
- Lightchain を10年使った人が自然に使える導線として、主要生成画面、ホーム、カテゴリ、履歴、Canvas、mobile が証跡付きで確認されている。
- 衣服/素材の upload -> 認識 -> cut/mask -> layer -> design placement -> Canvas/export の流れが直感操作として成立している。
- 失敗時UXが、技術者向けではなくユーザー向けの再試行/原因/次アクションを返す。
- production monitor / launch-ops / mass-market QA / security audit / performance checks / build/lint/typecheck が通る。10M級の最終呼称には、実負荷/同時実行/DB/Storage/SLO/alertingの追加証跡も必要。
- 法務・課金・外部公開など人間判断が必要なものは `goals/HUMAN_NEEDED.md` に分離され、実装完了と混同されない。
- `GOAL.md`、`STATE.md`、`plan.md` が最新証跡を指し、push済みと作業ツリーcleanはrelease gate summaryではなくgit status/remote readbackで別途確認する。

## Current Result

- G706 Gallery loading fallback / monitor refresh 2026-07-04: production monitor refresh initially exposed a Gallery UI probe false-negative while the screenshot still showed usable Gallery action cards during loading. `GalleryPage` now shows a stalled-loading state after the remote timeout window with `画像の読み込みに時間がかかっています`, `再読み込み`, and `新しく生成`, and `scripts/verify-launch-operations-readiness.mjs` now treats the visible `新しく生成` / `ローカル成果物` Gallery fallback signals consistently. Checks passed: `node --check scripts/verify-launch-operations-readiness.mjs`, `npm run typecheck -- --pretty false`, `npm run lint -- --max-warnings=0`, `npm run build`, and `git diff --check`. Current production UI monitor rerun with the actually deployed asset `assets/index.egBvsFYj.js` passed `uiOk=true` at `output/playwright/g706-production-monitor-current-prod-asset-r3/summary.json`, but overall monitor remains `ok=false` because recent generation/edge/usage failures still keep the 24h production monitor red. `output/playwright/g706-10m-completion-incomplete-r1/summary.json` still lists G617/G619/G669/G670/G674/H601/H602/G620/H602-completion/release-gate blockers, so public launch/all-ready remains not accepted.
- G705 AI fitting model-matrix production unblock 2026-07-04: `/fitting` の既定4パターン生成が本番の3 units/min制限を超えていたため、Fitting/Generate/Canvas の既定を `regular x 20s` 1パターンにし、UIとEdge Functionで3パターン超をブロックした。`model-matrix` Edge Functionも本番へ再デプロイ済み。可視Playwright CLI証跡 `output/playwright/g705-visible-fitting-prod-submit-post-deploy-r8-accepted/summary.json` は技術/UI proof として `technicalOk=true`, `jobStatus=completed`, `downloadedBytes=2402422`。目視対象 `output/playwright/g705-visible-fitting-prod-submit-post-deploy-r8-accepted/downloaded-6f737093-c517-4f54-8eed-6fe73da3805b.png` は白シャツが自然に着用され、襟・袖・前立て・裾の大きな破綻なし。最終受け入れproofは別途 `output/playwright/g705-visible-fitting-prod-visual-review-r1/visual-review.json` で、その目視承認が同じダウンロード画像SHA `05a5b3462056b5d600cb1ba608bdbd8de3b417c0a99830dce0a94216c6078163` に紐づくことを確認している。これはAIフィッティングの当該白シャツ経路の受け入れであり、G619/H602/public launch/全公開readyは別ブロッカーとして残す。
- G691 home-direction UI unification 2026-07-03: `/generate` and `/lightchain` keep the four-category entry while old intermediate `/lightchain/:toolId` selection pages are bypassed; Dashboard `START / まず1つ作る` and quick-workflow blocks are hidden/removed; Marketing Workspace, MaterialWorkbench, Canvas, and major workspace pages were restyled toward the `HEAVY CHAIN AI` black/cyan direction. Local proof passed: `npm run typecheck -- --pretty false`, `npm run lint -- --max-warnings=0`, `npm run build`, `git diff --check`, `npm run verify:internal-ux -- --out output/playwright/g691-home-direction-internal-ux-r2`, and strengthened `npm run verify:mass-market-qa -- --baseUrl http://127.0.0.1:4173 --out output/playwright/g691-home-direction-local-mass-market-r4 --authState output/playwright/g689-prod-temp-auth-r2/auth-state.json`. Visible screenshots: `output/playwright/g691-home-direction-visible-r1/01-dashboard.png`, `02-marketing-r2.png`, `03-canvas.png`. Production proof after `741024a` deploy passed on Zeabur asset `assets/index.CfGb4QXR.js`: `output/playwright/g691-prod-home-direction-mass-market-r1/SUMMARY.json`, `output/playwright/g691-prod-launch-ops-r4/summary.json`, and `output/playwright/production-monitor-post-g691-home-direction-r3/summary.json`. The launch-ops probe now targets the current labeled `ベースコンセプト` / `タイトル` fields instead of stale textarea assumptions. This does not close G617, G619, H601/H602, billing, checkout, payment, or public launch.
- Active window G601/G602/G603/G604/G605/G606/G607/G608/G609/G610/G611/G612/G613/G614/G615 is accepted in `GOAL.md`; G616 production deep rerun, G618 scale/ops baseline, and G620 security operations are accepted in this continuation.
- Key proof: `output/playwright/10m-product-readiness-g601/proof-reaudit.json`; `output/playwright/lightchain-workbench-parity-apparel-prod-20260626-r5-g602-final/SUMMARY.json`; `output/playwright/10m-product-readiness-g606/summary.json`; `output/playwright/10m-product-readiness-g608-security-audit/audit-readiness.json`; `docs/generation-quality-rubric-2026-06-26.md`.
- G603 proof: `output/playwright/g603-garment-layer-canvas-20260626T130426Z/SUMMARY.json` (`ok=true`, `failed=[]`) with screenshots, video, storage/body readback, masked PNG Canvas object, back-placement overlay coordinates, properties panel metadata, and exported PNG.
- G605 proof: `output/playwright/g605-onboarding-templates-20260626T133449Z/SUMMARY.json` (`ok=true`, `failed=[]`) with first-run Dashboard onboarding, Dashboard CTA href readback, Canvas empty-state proof, EC category -> design mode switching, EC size template persistence, product-card design template layer expansion, desktop/mobile videos, screenshots, storage readback, stale-preview guard, and process-exit/port-free cleanup proof.
- G607 proof: `output/playwright/10m-product-readiness-g607/release-gate-summary.json` has `ok=true`, `failed=[]`, `allowDirty=false`, blockers `[]`, all readbacks fresh/passing, and passing security audit, generation scorecard, syntax checks, typecheck, production build, lint, and `git diff --check`. `--allow-dirty` and `--skip-commands` were also verified as non-acceptance blockers.
- G609 packet: `docs/legal-safety-decision-packet-2026-06-26.md` defines upload-rights, commercial-use wording, copyright caveats, brand/likeness guardrails, retention policy questions, and exact H601 operator decisions. It does not auto-finalize legal policy.
- G610 proof: `output/playwright/g610-retention-project-search-20260626T144718Z/SUMMARY.json` has `ok=true`, `failed=[]`, 11 assertions, Dashboard project search by name, English/Japanese object type, empty/clear state, filtered delete, Canvas open route, storage readback, screenshot/video proof, and cleanup.
- G611 proof: `output/playwright/10m-product-readiness-g611-beta-scenarios-20260626-rerun4/SUMMARY.json` has `ok=true`, `failed=[]`, 17 desktop routes, 8 mobile routes, 25 videos, 15 upload routes with `upload_input_visible` and `upload_reflected_in_ui`, zero console/page/request failures, browser/context cleanup, and no submit/purchase/payment/checkout/external-publish/destructive actions. Scenario report: `docs/g611-beta-scenario-qa-2026-06-26.md`.
- G612 packet: `docs/g612-competitor-positioning-2026-06-26.md` compares Lightchain, Canva, Kittl, Photoroom, Adobe Firefly/Express, Runway, and Shopify Magic from current official pages, and recommends positioning Heavy Chain as an apparel-first AI production workspace rather than a generic image generator.
- G614 proof: `docs/g614-operations-runbook-2026-06-26.md` and `docs/rollback.md` now bind the approved-client Runway handoff path, daily monitor, release gate, failure triage, and human-approved rollback. `npm run verify:g614-ops` passed with required script/doc boundary checks, and Codex review found no remaining high/medium issues after fixes.
- G615 proof: `output/playwright/10m-product-readiness-g615/release-gate-summary.json` has `ok=true`, `failed=[]`, `allowDirty=false`, blockers `[]`, warnings `[]`, latest matching G611/G610 readbacks, production monitor, launch-ops, G603/G605/G606/G608 readbacks, and all command gates passing.
- G616 production deep proof: `output/playwright/g616-prod-deep-g603-garment-canvas-20260627/SUMMARY.json`, `output/playwright/g616-prod-deep-g605-onboarding-templates-20260627/SUMMARY.json`, and `output/playwright/g616-prod-deep-g610-retention-search-20260627/SUMMARY.json` all have `ok=true`, `failed=[]`, screenshots/videos, cleanup closed, and `baseUrl=https://heavy-chain.zeabur.app`.
- G617 exact blocker: latest MCP-bound Heavy Chain proof is `output/playwright/g617-same-run-fresh-generation-hc-g617-same-run-fresh-20260630T063740Z/runway-workspace-limit-after-successful-probe.json`. A minimal Codex-approved Runway MCP `gen-4` probe succeeded immediately before the run, but the first real G617 `campaign-image` request returned `account_limitation` / `workspace_limit` with only one G617 image in flight. Manifest created 10 marker-scoped jobs; `readback-after-runway-workspace-limit.json` showed `jobs=10`, `completedJobs=0`, `failedJobs=0`, `images=0`, `storage=0`, `signedUrlOk=0`; cleanup showed zero residual jobs/images/storage. A separate browser-site check succeeded at `output/playwright/g617-runway-site-direct-generation-20260630T0919Z/proof.json`, proving Runway web Gen-4 can generate one image, but it does not satisfy G617 because it is not bound to Heavy Chain jobs, MCP result JSON, local worker import/readback, all-10 same-run output, scorecard, or marker cleanup.
- G618 proof: `output/playwright/10m-product-readiness-g618/summary.json` has `ok=true`, 16 checks, blockers `0`, performance fixture `imageCount=1200`, `canvasObjectCount=600`, Gallery initial tiles `60`, Canvas persisted objects `600`, nonblank/colored Canvas pixels, export PNG `3348x8848`, `validPng=true`, edge color samples top `35140` / bottom `37609`, preview cleanup `groupAliveAfter=false`, production monitor 96h `ok=true`, failed jobs `0`, stale active `0`, storage signed URL `4/4`, edge failed/stale `0`, and no irreversible actions. Release-gate dry-run `output/playwright/10m-product-readiness-g618/release-gate-dry-run.json` read G618 as passed and failed only on expected `--allow-dirty` non-acceptance blocker. G618 is intentionally scoped to local synthetic scale plus read-only production ops readback, not real concurrent production load or new external alerting setup.
- G620 proof: `output/playwright/10m-product-readiness-g620/summary.json` has `ok=true`, 124 checks, blockers `0`, warnings `2`, runbook `docs/g620-security-operations-runbook-2026-06-30.md`, production monitor 96h `ok=true`, generation failure rate `0`, failed jobs `0`, stale active `0`, storage signed URL `4/4`, usage failed/stale `0`, edge failed/stale `0`, and no irreversible actions. The two warnings explicitly record that the 96h window had no usage_events / edge_function_runs live-row samples, while source/RPC completion and observability paths are checked. Release-gate dry-run `output/playwright/10m-product-readiness-g620/release-gate-dry-run.json` read G620 as passed and failed only on expected `--allow-dirty` non-acceptance blocker.
- G619 gate: `docs/g619-real-beta-evidence-packet-2026-06-30.md`, `scripts/create-g619-beta-session.mjs`, and `scripts/verify-g619-beta-evidence.mjs` define the consent-safe real-beta acceptance contract. G626 hardened the session workflow: `npm run create:g619-beta-session` now creates per-session participant instructions, operator checklist, consent, notes, redaction-review, readback, manifest entry, and artifact sha256 scaffolds without accepting G619; the verifier requires the instructions/checklist artifacts and redaction coverage. Scaffold proof `output/playwright/g619-real-beta-evidence-g626-scaffold-check/summary.json` is intentionally `ok=false` because real beta evidence is not collected. G619 remains queued until real consented sessions pass this verifier.
- H601 guard: `src/lib/legalSafetyGuard.ts` and `supabase/functions/_shared/legalSafety.ts` centralize upload-rights confirmation, commercial-use caveats, prompt safety appendix, and stricter brand/logo/person-likeness blocking. `/generate`, Canvas generation/edit modes, Chat generation/edit, Fitting/model-matrix, image API helpers, local worker queue, and generation Edge Functions now pass or enforce `legalSafety.rightsConfirmed`. `/terms`, `/privacy`, and `/legal` carry the same non-final caveats. `npm run verify:h601-legal-safety` passes. Production H601 UI readback now passes at `output/playwright/prod-h601-rights-check-20260701-r1/summary.json`; H601 still remains human-needed for final operator/legal policy decisions.
- Production all-site QA 2026-06-30/2026-07-01: `output/playwright/all-site-functions-prod-20260630T0925Z/SUMMARY.json` passed with `ok=true`, `failed=[]`, covering 17 desktop routes and 8 mobile routes on `https://heavy-chain.zeabur.app`. `output/playwright/all-site-functions-catalog-prod-20260630T0930Z/SUMMARY.json` passed 29 app routes, all 10 generation feature detail pages, and 28 Lightchain catalog entries. `output/playwright/all-10-generation-preflight-prod-20260630T0935Z/SUMMARY.json` confirmed all 10 feature pages load, accept upload, reflect the uploaded image, and show the generate button without clicking submit, but was before H601 UI deploy. Follow-up `output/playwright/prod-h601-rights-check-20260701-r1/summary.json` confirms current Zeabur `/generate?feature=campaign-image` shows H601 rights confirmation and caveat; `output/playwright/prod-domain-rights-check-20260630T0952Z/summary.json` still confirms `heavy-chain.com` is unreachable.
- Lightchain live-use follow-up 2026-06-30: `output/playwright/linkai-select-existing-asset-20260630/SUMMARY.md` now confirms the real `ChangeColor` existing-project flow beyond simple upload. Existing material selection exposed 18 `使用` actions; one existing asset was selected; `クリッピング` opened mask adjustment; `AIマスク認識` produced `トップス`; selecting `トップス` and pressing `抽出` returned to canvas with a cutout hoodie layer stacked over the original image; `次のステップ` and `OK` were exercised; selecting the post-extract layer exposed `クリッピング` and `次のステップ` again. Key evidence: `continue-clipping-select-mask-proof.json`, `ok-after-next-step-proof.json`, `post-extract-layer-controls-proof.json`, `33-mask-select-after-extract.png`, `40-after-ok.png`, and `42-post-extract-after-select-layer.png`. No purchase, checkout, external publish, destructive delete, or account-level irreversible action was performed.
- Heavy Chain Lightchain mask/layer parity follow-up 2026-06-30: `/lightchain` now exposes the same practical flow observed in Lightchain for garment references: `クリッピング`, `AIマスク認識`, mask candidates `トップス` / `無地部分` / `柄`, `抽出`, `次のステップ`, and an OK-ready preview. Canvas save now records original base, extracted cutout, and design overlay layers plus `maskPlan`, `layerPlan`, `compositionPreview`, selected candidate, extraction state, and next-step state. The shared `MaterialWorkbench` used by Generate/Marketing/Fitting/Studio/Models/Patterns/Video/Lab also has the same mask candidate, extract, reset, and next-step state path. Proof: `output/playwright/lightchain-mask-layer-flow-20260630-r4/SUMMARY.json` has `ok=true`, `failed=[]`, including candidate-change reset, re-extraction as `柄`, Canvas storage readback, screenshots, video, and cleanup. Regression proof: `output/playwright/g603-garment-layer-canvas-20260630-mask-layer-regression-r4/SUMMARY.json` has `ok=true`, `failed=[]`.
- Lightchain beginner UX simplification 2026-07-01: production now has beginner-first detail screens for 33 Lightchain-compatible tools. The default empty state is upload-only, and the advanced editing/saving controls appear after image upload. Proof: `output/playwright/lightchain-beginner-ux-20260701-r4/SUMMARY.json` (`ok=true`), screenshot `output/playwright/lightchain-beginner-ux-20260701-r4/01-desktop-empty.png` inspected visually, `output/playwright/lightchain-mask-layer-flow-20260701-simple-r7/SUMMARY.json` (`ok=true`), `output/playwright/g603-garment-layer-canvas-20260701-simple-r2/SUMMARY.json` (`ok=true`), `output/playwright/lightchain-all-feature-workflows-20260701-simple-r7/SUMMARY.json` (`ok=true`, 33 features), and production `output/playwright/prod-post-deploy-lightchain-20260701-r3/SUMMARY.json` (`ok=true`, screenshot inspected).
- Gallery loading fallback and H601-aware mass-market QA 2026-07-01: post-push production QA found `/gallery` could remain spinner-only while remote generated-image/signed-url readback was slow, and the mass-market verifier had not yet accounted for H601 rights confirmation before generation readiness. `GalleryPage` now renders the normal Gallery shell during loading, times out remote/signed-url reads after 10s, falls back to local workspace artifacts, shows warning + `再読み込み`, and keeps the empty-state `画像を生成する` path visible. `verify-mass-market-qa` now checks H601 copy and ticks the rights checkbox before asserting the `Runway workerで生成` button is ready without submit. Proof: local `output/playwright/local-post-fix-mass-market-20260701-r1/SUMMARY.json` (`ok=true`), production `output/playwright/prod-post-gallery-fix-mass-market-20260701-r1/SUMMARY.json` (`ok=true`, 17 desktop + 8 mobile routes, zero console/page/request failures, screenshots/videos, cleanup closed), and production Lightchain regression `output/playwright/prod-post-gallery-fix-lightchain-beginner-20260701-r1/SUMMARY.json` (`ok=true`). Commit `48f01d2` was pushed and Zeabur served `/assets/index.UhkYvOj9.js`.
- Production monitor / launch-ops refresh 2026-07-01: `verify-launch-ops` now matches current H601 and Gallery behavior: it fills campaign title by label, keeps `ベースコンセプト` intact, checks the H601 rights checkbox, waits for Gallery loading to settle, accepts usable fallback/empty-state proof, and ignores handled Supabase signed-url 544 fallback noise. Current proof `output/playwright/launch-operations-readiness-20260701-g623-r4/summary.json` is `ok=true`, `failed=[]`; current monitor `output/playwright/production-monitor-20260701-g623-r2/summary.json` is `ok=true`, blockers `0`, `uiOk=true`, generation failure rate `0`, stale active jobs `0`, storage errors `0`, with one non-blocking local worker inbox warning. `verify:release-gate` now reads these G623 proofs; dry-run `output/playwright/release-gate-g623-current-20260701-r1/summary.json` confirms `production monitor` and `launch operations` blockers are gone while older G610/G603/G605/G606/G608 readbacks, public domain, H602, and `--allow-dirty` remain.
- Fresh infrastructure readbacks 2026-07-01: G603, G605, G610, G606, and G608 are refreshed against the current code. Proofs: `output/playwright/g603-garment-layer-canvas-20260701-g624-r1/SUMMARY.json`, `output/playwright/g605-onboarding-templates-20260701-g624-r1/SUMMARY.json`, `output/playwright/g610-retention-project-search-20260701-g624-r1/SUMMARY.json`, `output/playwright/g606-performance-scale-20260701-g624-r2/summary.json`, and `output/playwright/g608-security-audit-20260701-g624-r2/audit-readiness.json`. G606 first caught a real fixture weakness (`canvas_export_bounds_too_small:3348x2688`); the stress grid now spans vertically and passes with export `3348x9948`, valid PNG, edge pixels present, and cleanup closed. Release gate now reads these G624 artifacts; `output/playwright/release-gate-g624-current-20260701-r1/summary.json` fails only on public domain, H602 billing completion, and `--allow-dirty`.
- Post-G624 production readback 2026-07-01: after push `eb2b7aa`, production monitor and mass-market QA were rerun against Zeabur. `output/playwright/production-monitor-post-g624-20260701-r1/summary.json` has `ok=true`, blockers `0`, storage errors `0`, and `uiOk=true`; `output/playwright/prod-post-g624-mass-market-20260701-r1/SUMMARY.json` has `ok=true`, `failed=[]`, 17 desktop routes, and 8 mobile routes. `output/playwright/10m-completion-audit-20260701-g624-r1/summary.json` remains intentionally `ok=false` for G617/G619/H601/H602/public-domain/H602 completion. Direct HTTP check confirms `https://heavy-chain.zeabur.app/` returns 200 and `https://heavy-chain.com/` still cannot connect on port 443.
- Post-G626 production UI readback 2026-07-01: after G626 push, production monitor and broad mass-market route QA were rerun against Zeabur. `output/playwright/production-monitor-post-g626-20260701-r1/summary.json` has `ok=true`, blockers `0`, storage errors `0`, and `uiOk=true`; `output/playwright/prod-post-g626-mass-market-20260701-r1/SUMMARY.json` has `ok=true`, `failed=[]`, 17 desktop routes, 8 mobile routes, zero console/page/request failures, screenshots/videos, upload-reflection states, H601-ready generation preflight without submit, and cleanup closed. This is current non-billing production UX proof, not full 10M completion.
- G627 release/completion gate wiring 2026-07-01: release gate and 10M completion audit now adopt `output/playwright/production-monitor-post-g626-20260701-r1/summary.json` and `output/playwright/prod-post-g626-mass-market-20260701-r1/SUMMARY.json` as the current production UX proof. Clean release gate `output/playwright/release-gate-g627-current-clean-20260701-r1/summary.json` passes all non-human readbacks and fails only on public domain and H602 completion; incomplete audit `output/playwright/10m-completion-audit-20260701-g627-r1/summary.json` has `g627_current_production_mass_market_qa` passed while preserving the G617/G619/H601/H602/domain blockers.
- Completion audit goal-range hardening 2026-07-01: `scripts/verify-10m-completion-audit.mjs` now requires accepted statuses through G628, including accepted-prod and accepted-local children, instead of stopping at G620. `output/playwright/10m-completion-audit-20260701-g628-r1/summary.json` confirms G621-G628 goal-status checks pass and still fails only for the real remaining G617/G619/H601/H602/domain/H602 completion/dependent gate blockers.
- Completion audit self-inclusion 2026-07-01: `scripts/verify-10m-completion-audit.mjs` now derives required accepted goals from all G6xx statuses in `GOAL.md`, so G629/G630 and future Goal Loop entries cannot be omitted from completion claims by forgetting to extend a fixed list. `output/playwright/10m-completion-audit-20260701-g630-r1/summary.json` confirms G621-G630 goal-status checks pass and still fails only for the real remaining G617/G619/H601/H602/domain/H602 completion/dependent gate blockers.
- Post-G630 production broad UX readback 2026-07-01: after push `0c60e6a`, production mass-market QA was rerun against Zeabur. `output/playwright/prod-post-g630-mass-market-20260701-r1/SUMMARY.json` has `ok=true`, `failed=[]`, 17 desktop routes, 8 mobile routes, zero console/page/request failures, screenshots/videos, upload-reflection states, H601-ready generation preflight without submit, and cleanup closed. Release/completion gates now read this proof plus `output/playwright/production-monitor-post-g630-20260701-r1/summary.json` as current production UX proof; clean release gate `output/playwright/release-gate-g631-current-clean-20260701-r1/summary.json` fails only on public domain and H602 completion.
- Incident response drill 2026-07-01: G632 adds `docs/g632-incident-response-drill-2026-07-01.md` and `npm run verify:g632-incident-response` for non-destructive response rehearsal. It covers Runway failure, worker stop, Storage readback failure, RLS/permission anomaly, and generation-quality regression with detection, first action, recovery rehearsal, proof, and stop condition. Proof `output/playwright/g632-incident-response-drill/summary.json` has `ok=true`, 5/5 scenarios passed, blockers `0`, and no irreversible actions touched; clean release gate `output/playwright/release-gate-g632-current-clean-20260701-r1/summary.json` reads G632 as passed and fails only on public domain and H602 completion.
- Production scale and alerting approval plan 2026-07-01: G633 adds `docs/g633-production-scale-alerting-plan-2026-07-01.md` and `npm run verify:g633-scale-alerting-plan`. It defines T0-T3 load tiers, proposed T3 targets, SLOs, cost cap requirement, stop conditions, alerting decision fields, and minimum alert signals without running load, setting up a paid vendor, changing DNS, enabling checkout/payment, publishing externally, or destructive cleanup. Proof `output/playwright/g633-scale-alerting-plan/summary.json` has `ok=true`, blockers `0`, and no irreversible actions touched; clean release gate `output/playwright/release-gate-g633-current-clean-20260701-r1/summary.json` reads G633 as passed and fails only on public domain and H602 completion.
- Post-G633 production readback 2026-07-01: after push `221830d`, production mass-market QA and monitor were rerun against Zeabur. `output/playwright/prod-post-g633-mass-market-20260701-r1/SUMMARY.json` has `ok=true`, `failed=[]`, 17 desktop routes, 8 mobile routes, zero console/page/request failures, screenshots/videos, H601-ready generation preflight without submit, and cleanup closed. `output/playwright/production-monitor-post-g633-20260701-r1/summary.json` has `ok=true`, blockers `0`, generation failure rate `0`, stale active jobs `0`, storage errors `0`, and `uiOk=true`; one local worker inbox warning remains non-blocking. Clean release gate `output/playwright/release-gate-g634-current-clean-20260701-r2/summary.json` fails only on public domain and H602 completion, and incomplete audit `output/playwright/10m-completion-audit-20260701-g634-r1/summary.json` marks G634 passed.
- Upload-first MaterialWorkbench UX 2026-07-01: G635 simplifies the shared material workbench empty state so Generate/Marketing/Fitting/Studio/Models/Patterns/Video/Lab do not show a checkerboard preview or visible mask/layer/Canvas structure controls before upload. After upload, AI mask recognition, extraction, layer selection, placement, sizing, notes, and Canvas structure return. Local proof `output/playwright/local-g635-material-workbench-empty-state-20260701-r2/SUMMARY.json` has `ok=true`, `failed=[]`, 17 desktop routes, 8 mobile routes, zero console/page/request failures, and screenshots inspected for desktop/mobile empty and uploaded states. Production readback `output/playwright/prod-post-g635-material-workbench-empty-state-20260701-r2/SUMMARY.json` passed after deploy, production monitor `output/playwright/production-monitor-post-g635-20260701-r1/summary.json` passed with blockers 0 and `uiOk=true`, release gate `output/playwright/release-gate-g635-current-clean-20260701-r1/summary.json` fails only on public domain/H602 billing completion, and audit `output/playwright/10m-completion-audit-20260701-g635-r1/summary.json` marks G635 passed.
- Mobile floating controls no-overlap 2026-07-01: G636 hides keyboard-shortcuts and feedback floating buttons on mobile so they no longer cover Lightchain/Generate cards. `scripts/verify-mass-market-qa.mjs` now checks `mobile_no_intrusive_floating_help_buttons` on every mobile route. Local proof `output/playwright/local-g636-mobile-floating-controls-20260701-r1/SUMMARY.json` and production proof `output/playwright/prod-post-g636-mobile-floating-controls-20260701-r1/SUMMARY.json` both have `ok=true`, `failed=[]`, 17 desktop routes, 8 mobile routes, zero console/page/request failures, and every mobile route reports no intrusive fixed help buttons. Production monitor `output/playwright/production-monitor-post-g636-20260701-r1/summary.json` has `ok=true`, blockers 0, and `uiOk=true`. Clean release gate `output/playwright/release-gate-g636-current-clean-20260701-r1/summary.json` fails only on public domain/H602 completion, and audit `output/playwright/10m-completion-audit-20260701-g636-r1/summary.json` marks G636 passed.
- Mobile Generate toolbar simplification 2026-07-01: G637 hides the Canvas-style select/drag/previous/next toolbar on mobile Generate while keeping it on desktop. `scripts/verify-mass-market-qa.mjs` now checks `mobile_generate_hides_canvas_toolbar`. Local proof `output/playwright/local-g637-mobile-generate-toolbar-20260701-r1/SUMMARY.json` and production proof `output/playwright/prod-post-g637-mobile-generate-toolbar-20260701-r1/SUMMARY.json` both have `ok=true`, `failed=[]`, 17 desktop routes, 8 mobile routes, zero console/page/request failures, H601 ready visible, and mobile Generate toolbar hidden. Production monitor `output/playwright/production-monitor-post-g637-20260701-r1/summary.json` has `ok=true`, blockers 0, and `uiOk=true`. Clean release gate `output/playwright/release-gate-g637-current-clean-20260701-r1/summary.json` fails only on public domain/H602 completion, and audit `output/playwright/10m-completion-audit-20260701-g637-r1/summary.json` marks G637 passed.
- Mobile Dashboard quick start 2026-07-01: G638 adds a mobile-only quick start row directly below the Dashboard greeting for Generate, Canvas, and Gallery. `scripts/verify-mass-market-qa.mjs` now checks `mobile_dashboard_has_above_fold_quick_start`. Local proof `output/playwright/local-g638-mobile-dashboard-quick-start-20260701-r1/SUMMARY.json` and production proof `output/playwright/prod-post-g638-mobile-dashboard-quick-start-20260701-r1/SUMMARY.json` both have `ok=true`, `failed=[]`, 17 desktop routes, 8 mobile routes, zero console/page/request failures, and the three quick-start links present. Production monitor `output/playwright/production-monitor-post-g638-20260701-r1/summary.json` has `ok=true`, blockers 0, and `uiOk=true`. Clean release gate `output/playwright/release-gate-g638-current-clean-20260701-r1/summary.json` fails only on public domain/H602 completion, and audit `output/playwright/10m-completion-audit-20260701-g638-r1/summary.json` marks G638 passed.
- Gallery calm fallback 2026-07-01: G639 removes the strong Gallery remote-read failure toast when the page can recover with the visible warning, reload button, local artifacts, and empty-state generate CTA. `scripts/verify-mass-market-qa.mjs` now checks `gallery_no_scary_remote_failure_toast` on desktop and mobile Gallery. Local proof `output/playwright/local-g639-gallery-calm-fallback-20260701-r1/SUMMARY.json`, production proof `output/playwright/prod-post-g639-gallery-calm-fallback-20260701-r1/SUMMARY.json`, and production monitor `output/playwright/production-monitor-post-g639-20260701-r1/summary.json` all pass. Clean release gate `output/playwright/release-gate-g639-current-clean-20260701-r1/summary.json` fails only on public domain/H602 completion, and audit `output/playwright/10m-completion-audit-20260701-g639-r1/summary.json` marks G639 passed.
- Mobile Dashboard dedupe 2026-07-01: G640 keeps the mobile above-fold quick-start row and hides the lower large duplicate Quick Actions cards on mobile, reducing repeated navigation and page length before Projects/Gallery. `scripts/verify-mass-market-qa.mjs` now checks `mobile_dashboard_hides_duplicate_quick_action_cards`. Local proof `output/playwright/local-g640-mobile-dashboard-dedup-20260701-r1/SUMMARY.json`, production proof `output/playwright/prod-post-g640-mobile-dashboard-dedup-20260701-r1/SUMMARY.json`, and production monitor `output/playwright/production-monitor-post-g640-20260701-r1/summary.json` all pass. Clean release gate `output/playwright/release-gate-g640-current-clean-20260701-r1/summary.json` fails only on public domain/H602 completion, and audit `output/playwright/10m-completion-audit-20260701-g640-r1/summary.json` marks G640 passed.
- Mobile Dashboard Lightchain compact 2026-07-01: G641 keeps the full Lightchain catalog on `/lightchain`, but the Dashboard-embedded mobile hub now shows 4 workflow candidates plus a `/lightchain` all-tools link. `scripts/verify-mass-market-qa.mjs` checks `mobile_dashboard_lightchain_has_all_tools_link`. Local proof `output/playwright/local-g641-mobile-dashboard-lightchain-compact-20260701-r1/SUMMARY.json`, production proof `output/playwright/prod-post-g641-mobile-dashboard-lightchain-compact-20260701-r1/SUMMARY.json`, and production monitor `output/playwright/production-monitor-post-g641-20260701-r1/summary.json` all pass. Clean release gate `output/playwright/release-gate-g641-current-clean-20260701-r1/summary.json` fails only on public domain/H602 completion, and audit `output/playwright/10m-completion-audit-20260701-g641-r1/summary.json` marks G641 passed.
- Mobile Dashboard activity summary 2026-07-01: G642 replaces the full mobile activity card stack with a three-metric summary and `/jobs` detail link, while preserving the detailed activity panels on tablet/desktop. `scripts/verify-mass-market-qa.mjs` checks `mobile_dashboard_activity_uses_compact_summary`. Local proof `output/playwright/local-g642-mobile-dashboard-activity-summary-20260701-r1/SUMMARY.json`, production proof `output/playwright/prod-post-g642-mobile-dashboard-activity-summary-20260701-r1/SUMMARY.json`, and production monitor `output/playwright/production-monitor-post-g642-20260701-r1/summary.json` all pass. Clean release gate `output/playwright/release-gate-g642-current-clean-20260701-r1/summary.json` fails only on public domain/H602 completion, and audit `output/playwright/10m-completion-audit-20260701-g642-r1/summary.json` marks G642 passed.
- Mobile Generate direct form 2026-07-01: G643 hides the assistant prompt/planning bar and project helper panel on mobile Generate detail routes so `/generate?feature=campaign-image` starts at the material upload and generation form. `scripts/verify-mass-market-qa.mjs` checks `mobile_generate_starts_at_material_form`. Local proof `output/playwright/local-g643-mobile-generate-direct-form-20260701-r1/SUMMARY.json`, production proof `output/playwright/prod-post-g643-mobile-generate-direct-form-20260701-r1/SUMMARY.json`, and production monitor `output/playwright/production-monitor-post-g643-20260701-r1/summary.json` all pass. Clean release gate `output/playwright/release-gate-g643-current-clean-20260701-r1/summary.json` fails only on public domain/H602 completion, and audit `output/playwright/10m-completion-audit-20260701-g643-r1/summary.json` marks G643 passed.
- Mobile Jobs bounded list 2026-07-01: G644 limits mobile Jobs to the first 5 visible jobs with a `さらにN件を表示` expander while preserving the full list on tablet/desktop. `scripts/verify-mass-market-qa.mjs` checks `mobile_jobs_initial_list_is_bounded`. Local proof `output/playwright/local-g644-mobile-jobs-bounded-list-20260701-r1/SUMMARY.json`, production proof `output/playwright/prod-post-g644-mobile-jobs-bounded-list-20260701-r2/SUMMARY.json`, and production monitor `output/playwright/production-monitor-post-g644-20260701-r1/summary.json` all pass; production r1 is retained only as deploy-transition stale-chunk evidence. Clean release gate `output/playwright/release-gate-g644-current-clean-20260701-r1/summary.json` fails only on public domain/H602 completion, and audit `output/playwright/10m-completion-audit-20260701-g644-r1/summary.json` marks G644 passed.
- Mobile Canvas fit on open 2026-07-01: G645 auto-fits existing Canvas content into the mobile initial viewport so saved/imported work opens centered above the bottom actions instead of clipped to the right; desktop behavior is unchanged. `scripts/verify-mass-market-qa.mjs` checks `mobile_canvas_content_fits_initial_view`. Local proof `output/playwright/local-g645-mobile-canvas-fit-20260701-r3/SUMMARY.json`, production proof `output/playwright/prod-post-g645-mobile-canvas-fit-20260701-r1/SUMMARY.json`, and production monitor `output/playwright/production-monitor-post-g645-20260701-r1/summary.json` all pass. Clean release gate `output/playwright/release-gate-g645-current-clean-20260701-r1/summary.json` fails only on public domain/H602 completion plus dirty-worktree allowance, and audit `output/playwright/10m-completion-audit-20260701-g645-r1/summary.json` marks G645 passed.
- Mobile Lightchain bounded tool list 2026-07-01: G646 limits mobile `/lightchain` initial category lists to 6 visible tools plus `さらにN件を表示`, while preserving category/search access to all tools and desktop full lists. `scripts/verify-mass-market-qa.mjs` checks `mobile_lightchain_tool_list_is_bounded`. Local proof `output/playwright/local-g646-mobile-lightchain-bounded-20260701-r1/SUMMARY.json`, production proof `output/playwright/prod-post-g646-mobile-lightchain-bounded-20260701-r1/SUMMARY.json`, and production monitor `output/playwright/production-monitor-post-g646-20260701-r1/summary.json` all pass. Release gate `output/playwright/release-gate-g646-current-clean-20260701-r1/summary.json` fails only on public domain/H602 completion plus dirty-worktree allowance, and audit `output/playwright/10m-completion-audit-20260701-g646-r1/summary.json` marks G646 passed.
- Credits actionable workspace panel 2026-07-01: G647 turns `/credits` from a sparse usage page into an actionable workspace panel with safe links back to `/generate` and `/jobs` plus rights-gate status, without adding checkout/payment flows. `scripts/verify-mass-market-qa.mjs` checks `credits_has_actionable_workspace_panel`. Local proof `output/playwright/local-g647-credits-actionable-20260701-r1/SUMMARY.json`, production proof `output/playwright/prod-post-g647-credits-actionable-20260701-r2/SUMMARY.json`, and production monitor `output/playwright/production-monitor-post-g647-20260701-r1/summary.json` all pass; production r1 is retained only as deploy-transition stale-chunk evidence. Release gate `output/playwright/release-gate-g647-current-clean-20260701-r1/summary.json` fails only on public domain/H602 completion plus dirty-worktree allowance, and audit `output/playwright/10m-completion-audit-20260701-g647-r1/summary.json` marks G647 passed.
- History mobile reuse actions 2026-07-01: G648 adds stable History action-panel proof, includes mobile History in mass-market QA, and bounds mobile timeline to the first 8 entries plus `さらにN件を表示`. Local proof `output/playwright/local-g648-history-reuse-actions-20260701-r2/SUMMARY.json`, production proof `output/playwright/prod-post-g648-history-reuse-actions-20260701-r2/SUMMARY.json`, and production monitor `output/playwright/production-monitor-post-g648-20260701-r1/summary.json` all pass; production r1 is retained only as deploy-transition 502 evidence. Release gate `output/playwright/release-gate-g648-current-clean-20260701-r1/summary.json` fails only on public domain/H602 completion plus dirty-worktree allowance, and audit `output/playwright/10m-completion-audit-20260701-g648-r1/summary.json` marks G648 passed.
- 10M completion audit: `scripts/verify-10m-completion-audit.mjs` and `npm run verify:10m-completion` fail-close the whole public-launch completion claim, separate production H601 UI readback from custom-domain reachability, and now require current post-G633 production mass-market QA proof plus all G6xx statuses present in `GOAL.md`. Latest incomplete audit `output/playwright/10m-completion-audit-20260701-g634-r1/summary.json` is intentionally `ok=false`; current production mass-market QA and production H601 rights readback pass, while blockers remain for G617, G619, H601 final human decision, H602, public custom domain, H602 completion, and dependent proof gates. The default G617 artifact directory is `output/playwright/g617-same-run-fresh-generation-hc-g617-same-run-fresh-20260630T063740Z`; a future successful rerun can still be selected with `--g617-dir <artifact-dir>`.
- Verification passed in this window: `npm run verify:error-messages`, `npm run verify:g603-garment-canvas`, `npm run verify:g605-onboarding-templates`, `npm run verify:g606-performance`, default and explicit `npm run verify:generation-scorecard` runs, remote false-pass negative scorecard, `npm run verify:g614-ops`, historical clean release-gate proof before production-H601 was added, `npm run security:audit`, `SECURITY_AUDIT_INCLUDE_LOCAL_ENV=1 npm run security:audit` negative detection, `bash scripts/supabase-prod-verify.sh`, `npm run typecheck`, `npm run lint -- --max-warnings=0`, `npm run build`, Deno checks for generation Edge Functions, `node --check scripts/verify-g603-garment-layer-canvas.mjs`, `node --check scripts/verify-g605-onboarding-templates.mjs`, `node --check scripts/verify-g614-operations-docs.mjs`, `node --check scripts/verify-10m-completion-audit.mjs`, `node --check scripts/verify-mass-market-qa.mjs`, `node --check scripts/verify-launch-operations-readiness.mjs`, `node --check scripts/verify-release-gate-unified.mjs`, `node --check scripts/audit-heavy-chain-goal-readiness.mjs`, `node --check scripts/measure-g606-performance.mjs`, `npm run verify:h601-legal-safety`, production H601 UI readback `output/playwright/prod-h601-rights-check-20260701-r1/summary.json`, local and production mass-market reruns `output/playwright/local-post-fix-mass-market-20260701-r1/SUMMARY.json` / `output/playwright/prod-post-gallery-fix-mass-market-20260701-r1/SUMMARY.json`, production Lightchain regression `output/playwright/prod-post-gallery-fix-lightchain-beginner-20260701-r1/SUMMARY.json`, current production launch-ops `output/playwright/launch-operations-readiness-20260701-g623-r4/summary.json`, current production monitor `output/playwright/production-monitor-20260701-g623-r2/summary.json`, fresh G624 infrastructure proofs, post-G624 production monitor `output/playwright/production-monitor-post-g624-20260701-r1/summary.json`, post-G624 mass-market QA `output/playwright/prod-post-g624-mass-market-20260701-r1/SUMMARY.json`, updated release-gate dry-runs `output/playwright/release-gate-g622-current-20260701-r2/summary.json`, `output/playwright/release-gate-g623-current-20260701-r1/summary.json`, `output/playwright/release-gate-g624-current-20260701-r1/summary.json`, and clean `output/playwright/release-gate-g624-current-clean-20260701-r1/summary.json`, updated incomplete 10M audits `output/playwright/10m-completion-audit-20260701-g622-r2/summary.json` and `output/playwright/10m-completion-audit-20260701-g624-r1/summary.json`, strict `npm run verify:10m-completion` expected failure with `ok=false`, `git diff --check`, and Codex high-effort review followed by fixes. Release/10M now intentionally fail until G617, G619, H601 final human decision, H602, public domain/entrypoint, and final release evidence close.
- Current honest status: non-billing/non-public product-readiness QA gate is passing, G619 now has an acceptance verifier, and the new 10M completion audit correctly blocks the stronger completion claim. Do not call the whole 10M public-launch objective complete until G617 is rerun successfully in a Runway workspace with available capacity, real G619 evidence is collected and passes, H601/H602 human decisions are closed, and strict `npm run verify:10m-completion` exits `ok=true`.

## Remaining Root Problems

- G617: same-run all-10 fresh generation is still not accepted. Runway website direct Gen-4 generation has been proven to work for one image, so the next restart should translate that availability into the approved MCP/local-worker path: use the Codex-approved existing Runway MCP client, local worker handoff/import, a new runId, no prior assets, and at most two concurrent Runway generations. The invalid `localhost:15554` / `mcp-remote` consent path must not be used as the restart path.
- G619: recorded QA is not enough for 10M public-launch readiness. Collect at least three consented real beta sessions covering desktop and mobile, at least two personas, and at least five required workflows; keep artifacts anonymized/redacted with sha256 values; then pass `npm run verify:g619-beta-evidence`.
- H601/H602: product-side legal/safety guard is implemented, but final Terms/Privacy wording, retention period, brand-reference policy, identifiable-person policy, copyright/marketing claims, pricing, refund/support, and public-release responsibility remain operator decisions. Billing/payment/checkout and external publishing stay disabled until those are closed.
- Production entrypoints: Zeabur production app currently passes broad non-submit QA, and production `/generate` now shows visible H601 rights-confirmation copy. `heavy-chain.com` is still down. Fix DNS/hosting or intentionally choose the canonical public URL before any public-launch claim.
- Production route UX: Zeabur production currently passes the latest broad non-submit mass-market QA, Gallery fallback readback, H601-ready generate route check, and Lightchain beginner detail-route regression. Continue using `https://heavy-chain.zeabur.app` as the verified production URL until the custom domain is fixed.

Strict completion condition: `npm run verify:10m-completion` must exit `ok=true`; until then Heavy Chain can be described only as non-billing/non-public product-readiness QA passing, not as fully ready for a 10M public launch.

## Full Remaining Execution Checklist

1. [blocked-exact:G617] Restore sustained image-generation availability in the connected approved Runway workspace. Evidence required: a new availability probe and at least one Heavy Chain-bound generation that can be imported by the local worker.
2. [blocked-exact:G617] Rerun same-run all-10 fresh generation with a new runId, no prior assets, Codex-approved existing Runway MCP client, local worker handoff/import, and at most two concurrent Runway generations.
3. [blocked-exact:G617] Complete worker/import/readback for all 10 major features: 10 completed jobs, 10 generated images, 10 Storage objects, signed URL readback OK, and no failed jobs.
4. [blocked-exact:G617] Open and score all 10 generated images with the generation-quality scorecard. Accept only all `pass`, or rerun bounded fixes for any `needs-polish` until pass.
5. [blocked-exact:G617] Run marker-scoped cleanup and post-cleanup readback for the G617 run, proving zero residual jobs/images/storage/usage artifacts outside retained local proof files.
6. [done-local:G619-ops] Harden the real-beta evidence plan: consent text, participant aliases, personas, required workflows, artifact location, redaction rules, stop conditions, participant instructions, and operator checklist are now generated and verifier-bound.
7. [human-needed:G619] Collect at least three consented real beta sessions. Coverage must include desktop and mobile, at least two personas, and at least five required workflows.
8. [queued:G619] For each beta session, store participant instructions, operator checklist, consent, notes, redaction-review, readback, local artifact sha256 values, and anonymized alias metadata.
9. [queued:G619] Run `npm run verify:g619-beta-evidence` and accept G619 only when it exits `ok=true`.
10. [human-needed:H601] Decide final Terms wording for commercial use, rights confirmation, generated output use, prohibited use, and no-clearance caveats.
11. [human-needed:H601] Decide final Privacy wording for uploaded materials, generated outputs, deletion requests, retention period, logs, and analytics.
12. [human-needed:H601] Decide brand-reference policy for third-party brand names, logos, trademarks, imitation, and comparative prompts.
13. [human-needed:H601] Decide identifiable-person and likeness policy for celebrities, real people, user-provided model images, and consent requirements.
14. [human-needed:H601] Decide copyright and marketing claims, especially whether Heavy Chain may say `commercial use allowed`, `rights cleared`, or similar claims.
15. [pending:H601] Reflect approved policy decisions into `/terms`, `/privacy`, `/legal`, `/generate`, and the relevant docs.
16. [pending:H601] Rerun `npm run verify:h601-legal-safety`, release-gate command wiring, and user-facing route checks after final wording changes.
17. [partial:H602] Billing quota behavior groundwork is implemented: normal accounts can be quota-gated again, Apple sandbox/tester accounts can bypass quota without real-charge proof, and usage events record H602 billing metadata. Human/operator still must confirm the exact Apple/payment price and attach readback proof.
18. [human-needed:H602] Confirm checkout/payment provider state, tax/invoice requirements, failed-payment UX, and account state transitions in the Apple/operator surface. Apple ID login, credentials, OTP/security prompts, purchase, and checkout/payment confirmation are human-only.
19. [human-needed:H602] Decide refund/support/SLA/incident communication policy.
20. [human-needed:H602] Decide external publishing scope: SNS posting, public gallery, share URLs, external integrations, and moderation responsibilities. Current implementation keeps public share-link creation/read disabled by default via UI and `HEAVY_CHAIN_PUBLIC_SHARE_ENABLED`.
21. [partial:H602] Billing quota enforcement and tester no-real-charge bypass are implemented locally; checkout/payment confirmation and external publishing remain blocked until explicit human/operator proof.
22. [partial:H602] `npm run verify:h602-billing` verifies billing-readiness/tester-bypass safety statically. Still required before production enablement: Apple sandbox transaction proof, no-real-charge proof for testers, rollback/readback logs, and final user-facing failure-state review.
23. [proposed:ops] Define production or production-equivalent concurrency/load test targets for users, generation jobs, DB/Storage/worker/SLO, cost bounds, and stop conditions.
24. [blocked:ops] Run approved load testing only after scope/cost/risk approval; keep billing, checkout, destructive cleanup, and external publish disabled unless separately approved.
25. [proposed:ops] Decide whether to add paid external alerting/monitoring. If approved, wire generation failure rate, stale jobs, Storage signing, Edge/RPC errors, DB health, and worker liveness alerts.
26. [queued:ops] Exercise incident response without destructive production changes: Runway failure, worker stop, Storage failure, RLS/permission anomaly, and generation-quality regression.
27. [blocked-exact:prod-entrypoint] Restore `https://heavy-chain.com` or intentionally replace it with the canonical public URL; then run route/readback proof against the chosen public entrypoint.
28. [done-prod:H601-ui-readback] Production Zeabur `/generate?feature=campaign-image` visibly matches the H601 local source guard for rights confirmation label, commercial caveat, and checkbox. Proof: `output/playwright/prod-h601-rights-check-20260701-r1/summary.json`. H601 final legal/operator decisions remain human-needed.
29. [done:prod-detail-route] Final upload-only Lightchain beginner detail screen was committed/pushed, deployed to Zeabur, and production saved-auth readback passed for `/lightchain/fitting-clothing-reference` at `output/playwright/prod-post-deploy-lightchain-20260701-r3/SUMMARY.json`.
30. [done-prod:gallery-fallback] Fix Gallery spinner-only dead end and update mass-market QA for H601 rights checkbox. Proof: `output/playwright/prod-post-gallery-fix-mass-market-20260701-r1/SUMMARY.json` and screenshots `gallery.png` / `mobile-gallery.png`.
31. [done-prod:monitor-launch-ops] Refresh production launch-ops and monitor for current H601/Gallery behavior. Proof: `output/playwright/launch-operations-readiness-20260701-g623-r4/summary.json`, `output/playwright/production-monitor-20260701-g623-r2/summary.json`, and release-gate dry-run `output/playwright/release-gate-g623-current-20260701-r1/summary.json`.
32. [done-local:infra-readbacks] Refresh G603/G605/G606/G608/G610 current readbacks and wire release gate to them. Proof: `output/playwright/release-gate-g624-current-20260701-r1/summary.json`.
33. [done-prod:post-g624-readback] Rerun production monitor and broad mass-market QA after G624 push; Zeabur passes, custom domain still fails. Proof: `output/playwright/production-monitor-post-g624-20260701-r1/summary.json`, `output/playwright/prod-post-g624-mass-market-20260701-r1/SUMMARY.json`, `output/playwright/10m-completion-audit-20260701-g624-r1/summary.json`.
34. [done-prod:post-g626-ui-readback] Rerun production monitor and broad mass-market QA after G626 push; Zeabur still passes. Proof: `output/playwright/production-monitor-post-g626-20260701-r1/summary.json`, `output/playwright/prod-post-g626-mass-market-20260701-r1/SUMMARY.json`.
35. [done-local:release-g627-wiring] Wire release/completion gates to current G627 production monitor and mass-market QA proof. Proof: `output/playwright/release-gate-g627-current-clean-20260701-r1/summary.json`, `output/playwright/10m-completion-audit-20260701-g627-r1/summary.json`.
36. [done-local:completion-audit-goal-range] Extend 10M completion audit accepted-goal requirements through G628 and accept `accepted-prod` / `accepted-local` statuses while preserving fail-close for queued/blocked/conflict. Proof: `output/playwright/10m-completion-audit-20260701-g628-r1/summary.json`.
37. [done-local:completion-audit-g629-self-inclusion] Replace the fixed accepted-goal list with GOAL.md-derived G6xx status auditing, so G629/G630 and future Goal Loop entries stay in the fail-close range. Proof: `output/playwright/10m-completion-audit-20260701-g630-r1/summary.json`, `output/playwright/production-monitor-post-g630-20260701-r1/summary.json`.
38. [done-prod:post-g630-broad-ux-readback] Rerun production broad mass-market QA after `0c60e6a` push and wire release/completion gates to current post-G630 production monitor and mass-market QA. Proof: `output/playwright/prod-post-g630-mass-market-20260701-r1/SUMMARY.json`, `output/playwright/production-monitor-post-g630-20260701-r1/summary.json`, `output/playwright/release-gate-g631-current-clean-20260701-r1/summary.json`.
39. [done-local:g632-incident-response] Add non-destructive incident response drill and release-gate wiring for Runway failure, worker stop, Storage readback failure, RLS/permission anomaly, and generation-quality regression. Proof: `output/playwright/g632-incident-response-drill/summary.json`, `output/playwright/release-gate-g632-current-clean-20260701-r1/summary.json`.
40. [done-local:g633-scale-alerting-plan] Define approval-required production/prod-equivalent load tiers, T3 targets, SLOs, cost bounds, stop conditions, and alerting decision packet. Proof: `output/playwright/g633-scale-alerting-plan/summary.json`, `output/playwright/release-gate-g633-current-clean-20260701-r1/summary.json`.
41. [done-prod:post-g633-production-readback] Rerun production monitor and broad mass-market QA after G633 push, and wire release/completion gates to current post-G633 production monitor and mass-market QA. Proof: `output/playwright/prod-post-g633-mass-market-20260701-r1/SUMMARY.json`, `output/playwright/production-monitor-post-g633-20260701-r1/summary.json`, `output/playwright/release-gate-g634-current-clean-20260701-r2/summary.json`, `output/playwright/10m-completion-audit-20260701-g634-r1/summary.json`.
42. [done-local:g635-upload-first-workbench] Simplify shared MaterialWorkbench empty state and add mass-market QA assertion that upload-first Generate does not expose advanced mask/Canvas action buttons before upload. Proof: local `output/playwright/local-g635-material-workbench-empty-state-20260701-r2/SUMMARY.json`, production `output/playwright/prod-post-g635-material-workbench-empty-state-20260701-r2/SUMMARY.json`, monitor `output/playwright/production-monitor-post-g635-20260701-r1/summary.json`, clean release gate `output/playwright/release-gate-g635-current-clean-20260701-r1/summary.json`, and incomplete audit `output/playwright/10m-completion-audit-20260701-g635-r1/summary.json`.
43. [done-prod:g636-mobile-floating-controls] Hide mobile keyboard/feedback floating controls and add QA assertion for no intrusive fixed help buttons on every mobile route. Proof: `output/playwright/local-g636-mobile-floating-controls-20260701-r1/SUMMARY.json`, `output/playwright/prod-post-g636-mobile-floating-controls-20260701-r1/SUMMARY.json`, `output/playwright/production-monitor-post-g636-20260701-r1/summary.json`, `output/playwright/release-gate-g636-current-clean-20260701-r1/summary.json`, `output/playwright/10m-completion-audit-20260701-g636-r1/summary.json`.
44. [done-prod:g637-mobile-generate-toolbar] Hide the mobile Generate Canvas-style toolbar and add QA assertion that it remains hidden on mobile campaign generation. Proof: `output/playwright/local-g637-mobile-generate-toolbar-20260701-r1/SUMMARY.json`, `output/playwright/prod-post-g637-mobile-generate-toolbar-20260701-r1/SUMMARY.json`, `output/playwright/production-monitor-post-g637-20260701-r1/summary.json`, `output/playwright/release-gate-g637-current-clean-20260701-r1/summary.json`, `output/playwright/10m-completion-audit-20260701-g637-r1/summary.json`.
45. [done-prod:g638-mobile-dashboard-quick-start] Add mobile Dashboard quick-start links for Generate, Canvas, and Gallery and verify they appear above the long workflow sections. Proof: `output/playwright/local-g638-mobile-dashboard-quick-start-20260701-r1/SUMMARY.json`, `output/playwright/prod-post-g638-mobile-dashboard-quick-start-20260701-r1/SUMMARY.json`, `output/playwright/production-monitor-post-g638-20260701-r1/summary.json`, `output/playwright/release-gate-g638-current-clean-20260701-r1/summary.json`, `output/playwright/10m-completion-audit-20260701-g638-r1/summary.json`.
46. [done-prod:g639-gallery-calm-fallback] Suppress scary Gallery remote failure toast when fallback/empty-state handles the failure and verify desktop/mobile Gallery no longer show the toast. Proof: `output/playwright/local-g639-gallery-calm-fallback-20260701-r1/SUMMARY.json`, `output/playwright/prod-post-g639-gallery-calm-fallback-20260701-r1/SUMMARY.json`, `output/playwright/production-monitor-post-g639-20260701-r1/summary.json`, `output/playwright/release-gate-g639-current-clean-20260701-r1/summary.json`, `output/playwright/10m-completion-audit-20260701-g639-r1/summary.json`.
47. [done-prod:g640-mobile-dashboard-dedup] Keep mobile Dashboard quick start above the fold and hide lower duplicate Quick Actions cards on mobile. Proof: `output/playwright/local-g640-mobile-dashboard-dedup-20260701-r1/SUMMARY.json`, `output/playwright/prod-post-g640-mobile-dashboard-dedup-20260701-r1/SUMMARY.json`, `output/playwright/production-monitor-post-g640-20260701-r1/summary.json`, `output/playwright/release-gate-g640-current-clean-20260701-r1/summary.json`, `output/playwright/10m-completion-audit-20260701-g640-r1/summary.json`.
48. [done-prod:g641-mobile-dashboard-lightchain-compact] Compact the Dashboard-embedded Lightchain hub on mobile to 4 visible candidates plus a `/lightchain` all-tools link, while preserving the full catalog on the Lightchain page. Proof: `output/playwright/local-g641-mobile-dashboard-lightchain-compact-20260701-r1/SUMMARY.json`, `output/playwright/prod-post-g641-mobile-dashboard-lightchain-compact-20260701-r1/SUMMARY.json`, `output/playwright/production-monitor-post-g641-20260701-r1/summary.json`, `output/playwright/release-gate-g641-current-clean-20260701-r1/summary.json`, `output/playwright/10m-completion-audit-20260701-g641-r1/summary.json`.
49. [done-prod:g642-mobile-dashboard-activity-summary] Replace the full mobile activity card stack with a compact summary plus `/jobs` detail link while preserving desktop detail panels. Proof: `output/playwright/local-g642-mobile-dashboard-activity-summary-20260701-r1/SUMMARY.json`, `output/playwright/prod-post-g642-mobile-dashboard-activity-summary-20260701-r1/SUMMARY.json`, `output/playwright/production-monitor-post-g642-20260701-r1/summary.json`, `output/playwright/release-gate-g642-current-clean-20260701-r1/summary.json`, `output/playwright/10m-completion-audit-20260701-g642-r1/summary.json`.
50. [done-prod:g643-mobile-generate-direct-form] Hide mobile Generate helper chrome so the campaign image route starts at the material upload/generation form. Proof: `output/playwright/local-g643-mobile-generate-direct-form-20260701-r1/SUMMARY.json`, `output/playwright/prod-post-g643-mobile-generate-direct-form-20260701-r1/SUMMARY.json`, `output/playwright/production-monitor-post-g643-20260701-r1/summary.json`, `output/playwright/release-gate-g643-current-clean-20260701-r1/summary.json`, `output/playwright/10m-completion-audit-20260701-g643-r1/summary.json`.
51. [done-prod:g644-mobile-jobs-bounded-list] Bound mobile Jobs to 5 initial rows with a show-all expander while preserving desktop full list. Proof: `output/playwright/local-g644-mobile-jobs-bounded-list-20260701-r1/SUMMARY.json`, `output/playwright/prod-post-g644-mobile-jobs-bounded-list-20260701-r2/SUMMARY.json`, `output/playwright/production-monitor-post-g644-20260701-r1/summary.json`, `output/playwright/release-gate-g644-current-clean-20260701-r1/summary.json`, `output/playwright/10m-completion-audit-20260701-g644-r1/summary.json`.
52. [done-prod:g645-mobile-canvas-fit-on-open] Fit mobile Canvas content into the initial viewport on open and add live fit proof to mass-market QA. Proof: `output/playwright/local-g645-mobile-canvas-fit-20260701-r3/SUMMARY.json`, `output/playwright/prod-post-g645-mobile-canvas-fit-20260701-r1/SUMMARY.json`, `output/playwright/production-monitor-post-g645-20260701-r1/summary.json`, `output/playwright/release-gate-g645-current-clean-20260701-r1/summary.json`, `output/playwright/10m-completion-audit-20260701-g645-r1/summary.json`.
53. [done-prod:g646-mobile-lightchain-bounded-tool-list] Bound mobile Lightchain category lists to 6 visible tools with a show-all expander while preserving all tools via category/search and desktop full lists. Proof: `output/playwright/local-g646-mobile-lightchain-bounded-20260701-r1/SUMMARY.json`, `output/playwright/prod-post-g646-mobile-lightchain-bounded-20260701-r1/SUMMARY.json`, `output/playwright/production-monitor-post-g646-20260701-r1/summary.json`, `output/playwright/release-gate-g646-current-clean-20260701-r1/summary.json`, `output/playwright/10m-completion-audit-20260701-g646-r1/summary.json`.
54. [done-prod:g647-credits-actionable-workspace-panel] Add safe next actions and rights-gate status to `/credits` while avoiding checkout/payment flows. Proof: `output/playwright/local-g647-credits-actionable-20260701-r1/SUMMARY.json`, `output/playwright/prod-post-g647-credits-actionable-20260701-r2/SUMMARY.json`, `output/playwright/production-monitor-post-g647-20260701-r1/summary.json`, `output/playwright/release-gate-g647-current-clean-20260701-r1/summary.json`, `output/playwright/10m-completion-audit-20260701-g647-r1/summary.json`.
55. [done-prod:g648-history-mobile-reuse-actions] Add stable History reuse-action proof, include mobile History in mass-market QA, and bound mobile timeline to 8 entries with show-all. Proof: `output/playwright/local-g648-history-reuse-actions-20260701-r2/SUMMARY.json`, `output/playwright/prod-post-g648-history-reuse-actions-20260701-r2/SUMMARY.json`, `output/playwright/production-monitor-post-g648-20260701-r1/summary.json`, `output/playwright/release-gate-g648-current-clean-20260701-r1/summary.json`, `output/playwright/10m-completion-audit-20260701-g648-r1/summary.json`.
56. [done-prod:g649-mobile-dashboard-focused-first-action] Add one mobile Dashboard primary next action, compact management links, and hide low-priority desktop-only panels on mobile while preserving destination pages. Proof: `output/playwright/local-g649-mobile-dashboard-focus-20260701-r1/SUMMARY.json`, `output/playwright/prod-post-g649-mobile-dashboard-focus-20260701-r1/SUMMARY.json`, `output/playwright/production-monitor-post-g649-20260701-r1/summary.json`, `output/playwright/release-gate-g649-current-clean-20260701-r1/summary.json`, `output/playwright/10m-completion-audit-20260701-g649-r1/summary.json`.
57. [done-prod:g650-brand-settings-readiness-panel] Add Brand Settings readiness panel with Brand/Runway/Team/Rights checks and safe next actions to Generate, Gallery, and Credits; no checkout/payment path added. Proof: `output/playwright/local-g650-brand-settings-readiness-20260701-r1/SUMMARY.json`, `output/playwright/prod-post-g650-brand-settings-readiness-20260701-r1/SUMMARY.json`, `output/playwright/production-monitor-post-g650-20260701-r1/summary.json`, `output/playwright/release-gate-g650-current-clean-20260701-r1/summary.json`, `output/playwright/10m-completion-audit-20260701-g650-r1/summary.json`.
58. [done-prod:g651-history-desktop-bounded-timeline] Bound desktop History timeline to 12 initial rows with a show-all expander while preserving mobile's 8-row bound. Proof: `output/playwright/local-g651-history-desktop-bounded-20260701-r1/SUMMARY.json`, `output/playwright/prod-post-g651-history-desktop-bounded-20260701-r1/SUMMARY.json`, `output/playwright/production-monitor-post-g651-20260701-r1/summary.json`, `output/playwright/release-gate-g651-current-clean-20260701-r1/summary.json`, `output/playwright/10m-completion-audit-20260701-g651-r1/summary.json`.
59. [done-prod:g652-model-library-clear-generation-flow] Add a Model Library production-flow panel with selected candidate, usage, material readiness, direct model-matrix generation, Canvas save/layering, and Gallery review; no checkout/payment path added. Proof: `output/playwright/local-g652-model-library-actions-20260701-r1/SUMMARY.json`, `output/playwright/prod-post-g652-model-library-actions-20260701-r2/SUMMARY.json`, `output/playwright/production-monitor-post-g652-20260701-r2/summary.json`, `output/playwright/release-gate-g652-current-clean-20260701-r1/summary.json`, `output/playwright/10m-completion-audit-20260701-g652-r1/summary.json`.
60. [done-prod:g653-pattern-workspace-production-flow] Add a Pattern Workspace production-flow panel with design-gacha generation, Canvas save/layering, and Gallery review; replace generic pattern previews with garment/small-goods mockup previews that show print area, placement, motif, and repeat signature; keep Credits next actions visible while usage details load. Proof: `output/playwright/local-g653-pattern-flow-20260701-r5/SUMMARY.json`, `output/playwright/prod-post-g653-pattern-flow-20260701-r1/SUMMARY.json`, `output/playwright/production-monitor-post-g653-20260701-r1/summary.json`, `output/playwright/release-gate-g653-current-clean-20260701-r1/summary.json`, `output/playwright/10m-completion-audit-20260701-g653-r1/summary.json`.
61. [done-prod:g654-video-workspace-production-flow] Add a Video Workspace production-flow panel with generation-instruction handoff, Canvas structure save, and Gallery material review; render the storyboard SVG in-page; replace empty shot placeholders with meaningful LOGO/PRODUCT/MODEL/CTA cards. Proof: `output/playwright/local-g654-video-flow-20260701-r2/SUMMARY.json`, `output/playwright/prod-post-g654-video-flow-20260701-r3/SUMMARY.json`, `output/playwright/production-monitor-post-g654-20260701-r1/summary.json`, `output/playwright/release-gate-g654-current-clean-20260701-r1/summary.json`, `output/playwright/10m-completion-audit-20260701-g654-r1/summary.json`.
62. [done-prod:g655-studio-workspace-production-flow] Add a Fashion Studio production-flow panel with model-matrix generation, Canvas save, and Gallery review; gate the Studio preview as a composition-context image containing model, pose, background, primary input, next step, and selected setup instead of a generic placeholder. Proof: `output/playwright/local-g655-studio-flow-20260701-r1/SUMMARY.json`, `output/playwright/prod-post-g655-studio-flow-20260701-r1/SUMMARY.json`, `output/playwright/production-monitor-post-g655-20260701-r1/summary.json`.
63. [done-prod:g656-lab-workspace-production-flow] Add a Lab production-flow panel with campaign generation, Canvas save, and Gallery review; render and gate the Lab evaluation preview as a decision image containing selected experiment, score signature, hypothesis/evaluation context, and next step instead of score-only cards. Proof: `output/playwright/local-g656-lab-flow-20260701-r1/SUMMARY.json`, `output/playwright/prod-post-g656-lab-flow-20260701-r1/SUMMARY.json`, `output/playwright/production-monitor-post-g656-20260701-r1/summary.json`.
64. [done-prod:g657-marketing-workspace-production-flow] Add a Marketing Workspace production-flow panel with campaign generation, Canvas save, and Gallery review; render and gate a marketing brief preview containing channel, template, copy, material kind, active layer, placement, and next step instead of text-only brief cards. Add `marketing` as a first-class workspace handoff source. Proof: `output/playwright/local-g657-marketing-flow-20260701-r1/SUMMARY.json`, adopted production `output/playwright/prod-post-g657-marketing-flow-20260701-r2/SUMMARY.json`, and `output/playwright/production-monitor-post-g657-20260701-r1/summary.json`. Production QA r1 passed route assertions but is not adopted because browser cleanup timed out.
65. [done-prod:g658-fitting-workspace-production-flow] Add a Fitting production-flow panel with model-matrix generation handoff, AI generation action state, and Gallery review; replace the meaningless preview with a gated fitting brief image containing workflow, body types, age groups, gender, material, layer, placement, pattern count, and next step. Add `fitting` as a first-class workspace handoff source. Proof: `output/playwright/local-g658-fitting-flow-20260701-r1/SUMMARY.json`, production `output/playwright/prod-post-g658-fitting-flow-20260701-r1/SUMMARY.json`, monitor `output/playwright/production-monitor-post-g658-20260701-r1/summary.json`, release gate `output/playwright/release-gate-g658-current-20260701-r1/summary.json`, and incomplete audit `output/playwright/10m-completion-audit-20260701-g658-r1/summary.json`.
66. [done-prod:g659-lightchain-order-preview-readback] Replace generic Lightchain workspace artifact previews with tool-specific order-sheet previews containing selected tool, request, reference, material/layer/placement/scale, mask candidate, outputs, and next step; bound remote workspace artifact saves to 8s local fallback so production save does not hang on remote artifact persistence. Proof: local `output/playwright/local-g659-lightchain-order-preview-20260701-r1/SUMMARY.json`, production `output/playwright/prod-post-g659-lightchain-order-preview-20260701-r2/SUMMARY.json`, monitor `output/playwright/production-monitor-post-g659-20260701-r1/summary.json`, release gate `output/playwright/release-gate-g659-current-20260701-r1/summary.json`, and incomplete audit `output/playwright/10m-completion-audit-20260701-g659-r1/summary.json`.
67. [done-prod:g660-dashboard-lightchain-direct-links] Make Dashboard Lightchain feature cards open their dedicated `/lightchain/:toolId` screens directly instead of updating a side-detail panel, and add QA coverage for direct detail-route links. Proof: local `output/playwright/local-g660-dashboard-lightchain-direct-links-20260701-r2/SUMMARY.json`, production `output/playwright/prod-post-g660-dashboard-lightchain-direct-links-20260701-r1/SUMMARY.json`, monitor `output/playwright/production-monitor-post-g660-20260701-r2/summary.json`, release gate `output/playwright/release-gate-g660-current-20260701-r1/summary.json`, and incomplete audit `output/playwright/10m-completion-audit-20260701-g660-r1/summary.json`.
68. [done-prod:g661-lightchain-direct-tool-screens] Make Lightchain tool cards open dedicated `/lightchain/:toolId` screens directly and verify desktop/mobile route assertions. Proof: local `output/playwright/local-g661-lightchain-direct-tool-links-20260701-r2/SUMMARY.json`; production coverage adopted from `output/playwright/prod-post-g662-gemini-provider-default-20260701-r3/SUMMARY.json` with desktop/mobile Lightchain direct-route assertions passed.
69. [done-prod-ui:g662-gemini-provider-default-groundwork] Switch the normal generation path to Gemini in source/config/docs, keep Runway as explicit fallback, add fail-closed Gemini Edge helper and provider readiness verification, and verify local/production UX shows `Geminiで生成`. Proof: `output/playwright/gemini-provider-readiness/summary.json`, `output/playwright/local-g662-gemini-provider-default-20260701-r1/SUMMARY.json`, production `output/playwright/prod-post-g662-gemini-provider-default-20260701-r3/SUMMARY.json`, plus passing smoke/typecheck/lint/build/Deno/diff/Supabase static checks. API key was not available, so live Gemini generation remains queued.
70. [done-local:g663-release-completion-gemini-wiring] Wire release/completion gates to G662 production Gemini UI readback and require `Geminiで生成` in desktop/mobile Generate proof. Proof: clean `output/playwright/release-gate-g663-current-clean-20260701-r1` fails only on public domain and H602 completion while all 23 commands pass; `output/playwright/10m-completion-audit-20260701-g663-r2/summary.json` preserves only real blockers.
71. [done-prod:g664-production-monitor-refresh] Rerun production monitor after G663 push and wire release gate to the latest monitor. Proof: `output/playwright/production-monitor-post-g663-20260701-r1/summary.json` is `ok=true`, blockers `0`, `uiOk=true`; clean `output/playwright/release-gate-g664-current-clean-20260701-r2` fails only on public domain and H602 completion while all 23 commands pass; `output/playwright/10m-completion-audit-20260701-g664-r1/summary.json` preserves only real blockers.
72. [done-prod-blocked:g665-gemini-live-path-hardening] Exercise production Gemini live generation path and reduce the blocker to credential validity. Added `npm run qa:g617-gemini-live` / `invoke-gemini`, updated completion audit to accept Gemini DB/Storage G617 artifacts, applied production migration `20260701143000_roll_free_subscription_period_for_usage_reservation.sql`, deployed `generate-image`, fixed H601 legal-safety `lv` substring false positive, and ran a production probe. Proof: `output/playwright/g617-gemini-live-generation-20260701-r5/run-manifest.json` reached Gemini and failed with `API_KEY_INVALID`; `readback-after-invalid-api-key.json`, `cleanup-after-invalid-api-key.json`, and `readback-after-cleanup-invalid-api-key.json` prove failed-job readback and zero residual jobs/images; clean `output/playwright/release-gate-g665-current-clean-20260701-r1` fails only on public domain/H602 completion; `output/playwright/10m-completion-audit-20260701-g665-r2/summary.json` preserves only known blockers. Checks passed: `npm run verify:h601-legal-safety`, `npm run verify:h602-billing`, `npm run verify:gemini-provider -- --out output/playwright/g665-gemini-live-path-hardening/summary.json`, `npm run verify:release-gate -- --out output/playwright/release-gate-g665-current-clean-20260701-r1`, `npm run verify:10m-completion -- --out output/playwright/10m-completion-audit-20260701-g665-r2 --allow-incomplete`, `npm run typecheck -- --pretty false`, `npm run lint -- --max-warnings=0`, `npm run build`, `deno check supabase/functions/_shared/legalSafety.ts supabase/functions/generate-image/index.ts`, `git diff --check`.
73. [done-prod:g666-production-monitor-refresh] Rerun production monitor after G665 and wire release gate to post-G665 monitor. Proof: `output/playwright/production-monitor-post-g665-20260701-r1/summary.json` is `ok=true`, blockers `0`, generation failure rate `0`, stale active jobs `0`, storage errors `0`, and `uiOk=true`; clean `output/playwright/release-gate-g666-current-clean-20260701-r2` reads the monitor as passed and fails only on public domain/H602 completion; `output/playwright/10m-completion-audit-20260701-g666-r1/summary.json` preserves only the known G617/G619/H601/H602/domain blockers.
74. [done-prod:g667-route-aware-loading-ux] Fix logged-in route loading so auth/lazy chunk waits do not collapse to a blank spinner, and make mass-market QA wait for the real route body before accepting proof. Proof: local `output/playwright/local-g667-route-aware-loading-20260701-r3/SUMMARY.json` `ok=true failed=[]`; production `output/playwright/prod-post-g667-route-aware-loading-20260701-r3/SUMMARY.json` `ok=true failed=[]` with 17 desktop routes, 9 mobile routes, zero console/page/request failures; production monitor `output/playwright/production-monitor-post-g667-20260701-r1/summary.json` `ok=true`, blockers `0`, storage errors `0`, and `uiOk=true`; clean `output/playwright/release-gate-g667-current-clean-20260701-r2` fails only on public domain/H602 completion; `output/playwright/10m-completion-audit-20260701-g667-r2/summary.json` preserves only known blockers.
75. [done-prod:g668-dashboard-recent-preview-recovery] Without using Gemini API, fix Dashboard recent generation so broken `画像なし` tiles are not shown as if production outputs are corrupted. Recent image cards now require a usable image URL and successful image load, unresolved/failed previews move into a recovery panel linking to Gallery and Jobs, and QA requires `dashboard_recent_images_no_broken_placeholders`. Proof: local `output/playwright/local-g668-dashboard-recent-preview-20260702-r2/SUMMARY.json` `ok=true failed=[]`; production `output/playwright/prod-post-g668-dashboard-recent-preview-20260702-r1/SUMMARY.json` `ok=true failed=[]` with 17 desktop routes, 9 mobile routes, zero console/page/request failures, and cleanup closed; production monitor `output/playwright/production-monitor-post-g668-20260702-r1/summary.json` `ok=true`, blockers `0`, storage errors `0`, and `uiOk=true`; clean `output/playwright/release-gate-g668-current-clean-20260702-r3` fails only on public domain/H602 completion; `output/playwright/10m-completion-audit-20260702-g668-r1/summary.json` preserves only known blockers.
76. [blocked-exact:g669-gemini-live-secret-and-g617] Use the AI Studio Gemini API key without writing it to source files. Done: direct text `generateContent` confirmed the key is valid; server-side Supabase Function secret `GEMINI_API_KEY` was set and verified by name only with update time `2026-07-01T16:56:13.365Z`; no `GEMINI_IMAGE_MODEL` override exists, so production uses code default `gemini-2.5-flash-image` / Nano Banana; direct image probes for Nano Banana, Nano Banana 2, Nano Banana Lite, Nano Banana Pro, and Imagen 4 all failed because this project has zero/free-tier image quota or paid-plan-required. Production Heavy Chain probe `g669-gemini-validkey-probe-20260701T165624Z:campaign-image` reached Gemini image generation and failed with the same quota layer; cleanup removed residual jobs/images. Proof: `output/playwright/g669-gemini-validkey-probe-20260701T165624Z/run-manifest.json`, `readback.json`, `cleanup.json`, `readback-after-cleanup.log`.
77. [human-needed:g669-gemini-image-quota] Add eligible Gemini API paid/prepay image-generation quota or provide another key/project whose image-generation quota is nonzero. Google Cloud free-trial credits alone do not make this project's AI Studio image free-tier quota nonzero. Do not store the key in source files.
78. [queued:g669-rerun-after-image-quota] After Gemini image quota is nonzero, rerun marker-scoped G617 live Gemini generation for all 10 features, DB/Storage/Gallery/Jobs/Canvas readback, visual scorecard, and cleanup.
79. [queued:g669-gemini-release-readback] After G617 live generation passes, rerun production monitor, production mass-market QA, release gate, and 10M completion audit, then update `STATE.md`, `GOAL.md`, and this plan with exact proof paths.
80. [queued:lightchain-regression] Re-run all 33 Lightchain-compatible workflow checks after the Gemini live change and confirm direct feature routes, upload, mask/extract/layer, Canvas save, and order-sheet previews still pass.
81. [queued:g619-beta] Collect/verify real consented beta evidence after internal use starts; run `npm run verify:g619-beta-evidence` and accept G619 only when it exits `ok=true`.
82. [queued:h601-h602] Keep H601/H602 human/legal/billing/public-publish items open until operator decisions and billing proof are complete.
83. [queued:release] Rerun `npm run verify:release-gate` after G617/G619/H601/H602/production-entrypoint updates and any ops changes.
84. [queued:release] Rerun strict `npm run verify:10m-completion` and require `ok=true`.
85. [queued:docs] Update `STATE.md`, `GOAL.md`, `plan.md`, and `goals/HUMAN_NEEDED.md` to the final accepted/closed state.
86. [queued:git] Review the dirty worktree and keep only intentional changes and proof artifacts.
87. [queued:review] Run Codex high-effort read-only review over the final 10M complete claim, safety boundaries, G617/G619/H601/H602, production entrypoints, and release evidence.
88. [queued:git] Commit the accepted changes in meaningful units after gates pass.
89. [queued:git] Push and capture remote readback; release-gate summaries alone do not prove push.
90. [queued:final-proof] Assemble the final evidence bundle: 10M audit summary, release gate, G617/Gemini live generation scorecard, G619 verifier, H601/H602 closure, production entrypoint proof, load/monitor proof if approved, cleanup proof, and git remote proof.
91. [queued:final] State `1000万人向けpublic launch ready` only after strict 10M audit is `ok=true`, human-needed items are closed, public entrypoint proof passes, and remote readback confirms the final state.
92. [done-local:g672-api-less-generation-readiness] APIキー未提供の間に進められる範囲を閉じた。OpenAI/Gemini/Imagen 選択UI、OpenAI Edge helper、環境変数ドキュメント、API key missing / quota / billing / mock disabled のユーザー向け失敗時UX、安全な `mock` provider、APIなしreadiness verifier、10主要機能分のモック成果物生成を追加した。`mock` provider は `ALLOW_MOCK_IMAGE_GENERATION=true` がない限り fail-closed で、本番実生成やG617完了証跡としては扱わない。Proof: `output/playwright/g672-api-less-generation-readiness/summary.json` `ok=true`, checks 12, failures 0, mockArtifacts 10; `npm run verify:api-less-generation`, `deno check supabase/functions/_shared/geminiModels.ts supabase/functions/_shared/geminiImage.ts supabase/functions/_shared/openaiImage.ts supabase/functions/_shared/mockImage.ts supabase/functions/generate-image/index.ts`, `npm run typecheck -- --pretty false`, `npm run lint -- --max-warnings=0`, `npm run build`, `npm run smoke:edge` passed.
93. [queued:g672-live-after-api-keys] APIキー提供後に、まず OpenAI 1枚、Gemini/Imagen 1枚の marker-scoped live probe を実行し、DB/Storage/Gallery/Jobs/readback/cleanup を確認する。成功後に全10機能G617へ進む。失敗時は exact blocker を `API_KEY_INVALID` / `quota_or_billing_required` / `model_unavailable` / `storage_or_persistence_failure` に分類する。
92. [in-progress:g670-runway-return-master-plan] Switch the active G617 closure path back to Runway because Gemini image generation is blocked by free-tier image quota `limit: 0`. Use only the approved existing Runway MCP client plus Heavy Chain local-worker import; do not use `localhost:15554` / `mcp-remote` consent, direct Runway API, billing, purchase, checkout, external publish, or prior generated assets as completion proof. Goal set in Codex: `Runway復帰ルートでHeavy ChainのG617 same-run fresh全10機能生成を完了させ、worker/import/readback/visual scorecard/cleanup/release・completion証跡更新まで実行する`.
93. [in-progress:g670-research-old-failures] Re-read the old G617 evidence before retrying. Confirmed so far: `output/playwright/g617-same-run-fresh-generation-hc-g617-same-run-fresh-20260630T063740Z/runway-workspace-limit-after-successful-probe.json` shows a minimal `gen-4` probe succeeded, then first real G617 `campaign-image` returned `workspace_limit`; `run-manifest.json` contains all 10 intended jobs and result JSON paths; NisenPrints proof `output/runway-local-worker/nisenprints-mcp-result-import-20260624/worker-stdout.json` and `readback.json` prove the local worker can import a Runway MCP result JSON into Heavy Chain DB/Storage.
94. [queued:g670-runway-current-availability] Check current approved Runway client availability without creating G617 jobs first. Required proof: connected account/workspace identity if callable, at least one successful current image generation or exact blocker, selected model (`gen-4`, `gpt-image-2`, or `nano-banana-pro`), and note whether sustained queue capacity can support 10 jobs at max 2 concurrent generations.
95. [queued:g670-g617-new-run] Create a fresh G617 runId and marker-scoped Heavy Chain jobs only after availability is positive. Required constraints: no past assets, 10 major features, result JSON path for each job under `output/runway-mcp-results/inbox`, max 2 Runway generations in flight, and immediate stop/cleanup on `workspace_limit` or model unavailable.
96. [queued:g670-handoff-worker] Build handoff JSON/prompt files with `npm run build:local-runway-handoff`, start `npm run worker:local-runway:watch`, and process Runway result JSONs from `output/runway-mcp-results/inbox`. Required proof: worker stdout/manifest, processed result files, and no stale inbox confusion.
97. [queued:g670-runway-generate-all-10] Generate/import all 10 feature outputs through the approved Runway MCP client. Required proof: 10 completed jobs, 10 generated image rows, 10 Storage objects, signed URL readback OK, 0 failed jobs, and Gallery/Jobs/Canvas/History readback.
98. [queued:g670-scorecard-and-polish] Run `npm run verify:generation-scorecard` on the new G617 artifact directory. Required proof: all 10 images are `pass`; if any `needs-polish` or `fail`, rerun only the affected feature with a fresh marker result and repeat readback/scorecard.
99. [queued:g670-cleanup] Run marker-scoped cleanup and post-cleanup readback for the new G617 run. Required proof: zero residual jobs/images/storage/usage artifacts for that marker, with local proof images/manifests retained.
100. [queued:g670-regression-release] After G617 passes, rerun Lightchain all-33 workflow regression, production monitor, production mass-market QA, release gate, and strict 10M completion audit. Update `STATE.md`, `GOAL.md`, `plan.md`, and `goals/HUMAN_NEEDED.md` with exact accepted/blocker proof.
101. [in-progress:g671-codex-app-runway-mcp-client] Configure this Codex app as the approved Runway MCP client instead of using arbitrary direct API access. Done so far: created personal plugin `/Users/nichikatanaka/plugins/runway-mcp`, installed it as `runway-mcp@personal`, validated the manifest, and verified `codex plugin list` reports `installed, enabled`. The plugin points to official `https://mcp.runwayml.com/mcp` through `mcp-remote@0.1.37`, stores auth under `/Users/nichikatanaka/.mcp-auth`, and uses callback port `15555` to avoid the already-listening `15554` client. User-side Codex app settings screenshot shows `runway` MCP server enabled. Proof: `output/playwright/g671-codex-runway-mcp-plugin-20260702-r3/summary.json` and `output/playwright/g671-codex-app-runway-mcp-client-20260702-r1/proof.json`; r3 confirms `15555` is currently served by `mcp-remote https://mcp.runwayml.com/mcp`. Current limitation: this already-open thread still does not expose Runway MCP tools, so a new/reloaded Codex app session must load the plugin before G617 generation can proceed.
102. [queued:g671-runway-tool-exposure-proof] In the new/reloaded Codex app session, prove Runway tools are actually visible with a harmless tools/list or equivalent availability probe. If Runway OAuth/organization approval appears, stop for user approval and record the exact blocker; do not bypass CAPTCHA/OTP/security checks.
103. [queued:g671-bridge-readiness-reclassification] Do not treat the old hosted bridge `/tools` 502 as blocking the new Codex app MCP route. Keep `verify:runway-readiness` failure as legacy bridge evidence until either the verifier is updated for the Codex app plugin route or a separate G671 proof artifact records plugin tool exposure and current Runway availability.
104. [done-local:g671-runway-mcp-tools-and-account-proof] After user OAuth approval, Runway MCP non-generation probes succeeded through the Codex app/mcp-remote route. `tools/list` returned 11 tools including `whoami`, `list_workspaces`, `generate_image`, `get_task`; `whoami` confirmed current workspace `Soy`, no alternate workspaces, and available image models `nano-banana-pro`, `gpt-image-2`, `gen-4`. Proof: `output/playwright/g671-runway-mcp-tools-list-probe-20260702-r2/proof.json`, `output/playwright/g671-runway-mcp-account-probe-20260702-r1/proof.json`.
105. [blocked-exact:g670-runway-generation-submit-workspace-limit] Created fresh G617 run `hc-g617-runway-codexapp-20260701T172658Z` with 10 Heavy Chain pending jobs and result JSON inbox paths. The first Heavy Chain-bound `campaign-image` generation failed before task creation with `account_limitation/workspace_limit` for `nano-banana-pro`, `gen-4`, and `gpt-image-2`. Readback showed `jobs=10`, `completedJobs=0`, `images=0`, `storage=0`; cleanup removed all jobs/images with zero residuals. Proof: `output/playwright/g617-runway-codexapp-generation-hc-g617-runway-codexapp-20260701T172658Z/run-manifest.json`, `runway-mcp-generate-r1/proof.json`, `runway-mcp-generate-gen4-r1/proof.json`, `runway-mcp-generate-gptimage2-r1/proof.json`, `readback.json`, `cleanup.json`.
106. [queued:g670-rerun-after-runway-workspace-limit] After the connected Runway workspace can submit at least one image generation, rerun with a new G617 runId using `npm run run:g617-runway-mcp`, max two active generations, save result JSON to `output/runway-mcp-results/inbox`, run local worker import/readback, scorecard, cleanup, release gate, and strict 10M completion audit.
107. [done-local:g673-api-wait-ui-experience-readiness] APIキー未提供の間に、初心者向けUI体験、主要ページ、生成モデル選択、upload-first導線、Lightchain互換33機能、マスク/レイヤー/Canvas保存、desktop/mobileレイアウトをローカル実ブラウザで確認した。必要調整として `verify-mass-market-qa` の生成ボタン判定を旧 `Geminiで生成` 固定から、Imagen/Gemini/OpenAIのモデル選択後文言へ更新した。Proof: `output/playwright/g673-local-ui-experience-lightchain-all-r1/SUMMARY.json` `ok=true`, `featureCount=33`, `failed=[]`; `output/playwright/g673-local-ui-experience-mass-market-r3/SUMMARY.json` `ok=true`, `failed=[]`; key screenshots visually inspected: `generate-campaign.png`, `mobile-generate-campaign.png`, `generate-campaign-uploaded.png`, `mobile-generate-campaign-uploaded.png`, and Lightchain `desktop-index.png`.
108. [blocked-exact:g674-openai-direct-probe] User-provided OpenAI project key was tested with a one-image direct `gpt-image-2` probe without writing the key to source files. The request reached OpenAI but failed before image creation with `billing_hard_limit_reached` / `billing_limit_user_error`. Do not set this key into Supabase production secrets until the OpenAI account billing hard limit is resolved, because Heavy Chain would hit the same blocker. Proof: `output/playwright/g674-openai-direct-probe/summary.json` `ok=false`, `status=400`, `exactBlocker=billing_hard_limit_reached`, `imagePath=null`.
109. [done-local:g676-openai-direct-provider-probe] A newly provided OpenAI project key was tested with a one-image direct `gpt-image-2` probe without writing the key to source files or proof artifacts. The request succeeded with `status=200` and produced a usable product image. Proof: `output/playwright/g676-openai-direct-probe-newkey/summary.json` `ok=true`, `status=200`, `imagePath=output/playwright/g676-openai-direct-probe-newkey/openai-gpt-image-2-newkey.png`; image visually inspected as a clean black hoodie product image with chain detail, no visible text/watermark.
110. [done-prod:g677-openai-server-secret-and-marker-probe] `OPENAI_IMAGE_API_KEY` and `OPENAI_IMAGE_MODEL=gpt-image-1-mini` were set only as server-side Supabase Function secrets, `generate-image` was redeployed, and the low-cost OpenAI Heavy Chain marker probe passed for `campaign-image`. Proof: `output/playwright/g677-openai-mini-campaign-probe/run-manifest.json`, `readback.json`, `generated-openai-mini-campaign.png`, `cleanup.json`, and `readback-after-cleanup.json`; readback showed 1 completed job, 1 image, 1 Storage object, signed URL OK, then cleanup returned 0 jobs/images.
111. [done-prod:g677-openai-mini-all-10-split-proof] To minimize paid usage, the all-10 OpenAI mini proof was completed as a split run rather than rerunning a strict same-run G617 after a false H601 prompt blocker. First run generated `campaign-image`, `product-shots`, and `model-matrix`; remaining run generated `design-gacha`, `scene-coordinate`, `multilingual-banner`, `remove-bg`, `colorize`, `upscale`, and `variations`. DB/Storage/readback/cleanup passed for both runs. The first `remove-bg` visual was weak, so a one-feature `gpt-image-1-mini` polish run generated a clean white-background ecommerce cutout-style result and cleanup returned 0 residual jobs/images. Visual scorecard: 10 pass, verified by `npm run verify:generation-scorecard -- --scorecard output/playwright/g677-openai-mini-low-cost-proof/visual-scorecard.json --readback output/playwright/g677-openai-mini-low-cost-proof/readback-merged-before-cleanup.json --expected-features campaign-image,product-shots,model-matrix,design-gacha,scene-coordinate,multilingual-banner,remove-bg,colorize,upscale,variations`. Proof: `output/playwright/g677-openai-mini-all10/*`, `output/playwright/g677-openai-mini-remaining7/*`, `output/playwright/g678-openai-mini-remove-bg-polish/*`, `output/playwright/g677-openai-mini-low-cost-proof/visual-scorecard.json`, `readback-merged-before-cleanup.json`, and `scorecard.json`.
112. [queued:g617-strict-openai-same-run-if-needed] If a strict same-run G617 artifact is required, rerun all 10 features in one OpenAI mini run, then rerun release gate and strict 10M completion audit. Until then, G617 is not fully accepted as same-run all-10 because the accepted visual proof is split-run plus one-feature polish.
113. [in-progress:g678-internal-use-handoff] Prepare the current OpenAI mini Heavy Chain path for internal use without claiming full public-launch completion. Required before handoff: review dirty worktree and secret hygiene; run scorecard, H601, API-less readiness, typecheck, Deno check, lint/build if feasible, and diff check; run a read-only Codex review; commit and push intentional changes; then run production monitor and broad production mass-market QA after deploy settles.
114. [queued:g678-post-deploy-readback] After GitHub push triggers Zeabur deploy, confirm production still serves the expected app and run `npm run monitor:production` plus `npm run verify:mass-market-qa` against production. Update `STATE.md`, `GOAL.md`, and this plan with exact proof paths. If deploy is still settling, retry once before marking a blocker.
115. [queued:g678-internal-beta-pack] For社内利用, package the accepted current state as: OpenAI mini generation path accepted by split-run scorecard, Lightchain direct screens accepted, upload/mask/layer/Canvas flows accepted by local/prod QA, and explicit known blockers. Do not tell internal users that public launch is complete; tell them strict same-run G617, real G619 beta evidence, H601/H602 decisions, and public domain remain open.
116. [queued:g617-strict-cost-gated] Keep strict same-run OpenAI all-10 G617 as cost-gated optional work. Run only if final acceptance requires same-run proof rather than the G677 split-run plus polish proof, because the current split proof already covers all 10 feature outputs at lower cost.
117. [done-prod:g678-internal-use-handoff-readback] G678 internal-use handoff checks passed after push `16d2a73` and follow-up monitor hardening. Checks passed: `npm run verify:generation-scorecard`, `npm run verify:h601-legal-safety`, `npm run verify:api-less-generation`, `npm run typecheck -- --pretty false`, Deno check for Gemini/OpenAI/mock/generate-image, `npm run lint -- --max-warnings=0`, `npm run build`, `git diff --check`, and read-only Codex reviews. Production proof: broad mass-market QA `output/playwright/prod-post-g678-openai-mini-mass-market-20260702-r1/SUMMARY.json` is `ok=true failed=[]`; monitor r3 `output/playwright/production-monitor-post-g678-openai-mini-20260702-r3/summary.json` is `ok=true`, blockers `0`, `uiOk=true`, generation failure rate `0`, stale active jobs `0`, storage errors `0`. r1/r2 monitor failures were not product failures; they exposed stale `Geminiで生成` monitor expectations after the new model selector, now fixed to accept the actual variable generation button names without weakening to plain navigation text.
118. [pattern:g678-repeatable-first-time-success-loop] Promote the successful low-cost provider loop for future initial unknowns: direct provider 1-image probe without storing secrets in source; set only server-side secrets; deploy/confirm the Edge function; run one Heavy Chain marker probe with DB/Storage/signed URL/cleanup; run all features in the cheapest safe split when cost matters; polish only weak feature outputs; merge readback and scorecard; run production monitor and mass-market QA; update source-of-truth docs with exact accepted/blocker scope. This pattern is reusable, but it must not convert split-run proof into strict same-run G617 acceptance unless a fresh same-run all-10 artifact exists.
119. [done-local:g679-real-cutout-layer-stack] Replace the fake extracted-layer preview with an actual browser-canvas transparent PNG cutout path. Shared MaterialWorkbench now generates and stores `extractedImageUrl`, `cutoutBounds`, and `maskEngine`; Lightchain detail extraction uses the same utility at interaction time; workspace handoff saves original-base and extracted-cutout as separate Canvas image objects when an extracted material exists; `verify-lightchain-mask-layer-flow` now verifies PNG data URL, real alpha transparency, cutout bounds/engine metadata, original/extracted/overlay stack, reset behavior, and Canvas localStorage readback. Proof: `output/playwright/lightchain-mask-layer-flow-20260702T063042Z/SUMMARY.json` `ok=true failed=[]`; checks passed: `node --check scripts/verify-lightchain-mask-layer-flow.mjs`, `npm run typecheck -- --pretty false`, `npm run lint -- --max-warnings=0`, `npm run build`, `npm run verify:lightchain-mask-layer`.
120. [queued:g679-production-readback-after-push] After push/deploy, rerun a production Lightchain mask/layer proof or broad production mass-market QA if needed. Local G679 is accepted, but production readback is not yet claimed until the pushed asset is served.
121. [done-local:g680-post-g679-required-hardening] Closed the "must do without being asked" follow-up after G679 locally: extracted transparent PNG cutouts now have bounded storage behavior and fail closed above the local Canvas storage limit, metadata records `cutoutOutputSize`, `cutoutDataUrlBytes`, `cutoutMaxDataUrlBytes`, and `cutoutStoragePolicy`, and the Lightchain mask/layer verifier reads the saved Canvas object back for bounded data URL, storage policy, real alpha transparency, layer stack, reset behavior, save-preflight-before-project-creation ordering, and first-time upload -> AI mask -> extract -> Canvas save flow. Proof: `output/playwright/g680-local-lightchain-mask-layer-storage-r6/SUMMARY.json` `ok=true failed=[]`, `cutoutDataUrlBytes=3353`, `cutoutMaxDataUrlBytes=750000`, storage policy `bounded-local-canvas-data-url-v1`.
122. [done-prod:g680-production-readback] After push `dd4c3f7` and Zeabur deploy settling, production `/lightchain/fitting-clothing-reference` mask/extract/layer/Canvas proof passed against `https://heavy-chain.zeabur.app`. Proof: `output/playwright/g680-prod-lightchain-mask-layer-storage-r2/SUMMARY.json` `ok=true failed=[]`, no console/page/request failures, real alpha transparency, bounded cutout data URL, matching nested metadata, 33 feature detail routes, and cleanup closed. r1 is retained only as deploy-transition stale-chunk evidence.
123. [done:g680-docs-review-release] `STATE.md`, `GOAL.md`, and this plan now record exact local/prod proof paths. Checks passed: `npm run typecheck -- --pretty false`, `npm run lint -- --max-warnings=0`, `npm run build`, `node --check scripts/verify-lightchain-mask-layer-flow.mjs`, `git diff --check`, local/prod Lightchain verifier, and read-only Codex review `no issues`. G617/G619/H601/H602/public-domain blockers remain separate from the G680 storage/readback hardening scope.
124. [done-prod:g681-internal-beta-ui-consistency] Implemented and production-verified the Heavy Chain internal-beta UI/UX consistency pass. Dashboard now leads with `まず1つ作る` and three CTAs, Generate uses provider-neutral primary action text, Lightchain detail screens are presented as `素材ワークベンチ`, Canvas/Gallery/empty states use calmer language, feedback captures `どこを押すかわからない` / `生成結果が微妙` / `保存先がわからない` / `動作が遅い` / `その他`, and `npm run verify:internal-ux` guards old user-facing label regressions. Local proof: `output/playwright/g681-internal-ux-consistency-r7/summary.json`, `output/playwright/g681-local-internal-ux-mass-market-r5/SUMMARY.json`, `output/playwright/g681-local-lightchain-mask-layer-r3/SUMMARY.json`, `output/playwright/g681-gemini-provider-label-readiness-r4/summary.json`, `output/playwright/g681-local-generation-scorecard-r1`, plus screenshot review of Dashboard/Generate/Lightchain/Canvas/Gallery/mobile Dashboard. Production proof: `output/playwright/g681-prod-internal-ux-mass-market-r2/SUMMARY.json` and `output/playwright/production-monitor-post-g681-20260702-r2/summary.json`. Checks passed: old-label `rg` search, typecheck, lint, build, `git diff --check`, `node --check scripts/verify-launch-operations-readiness.mjs`, and `npm run verify:internal-ux -- --out output/playwright/g681-internal-ux-consistency-r8`. Codex review was attempted but blocked by usage/model availability. G617/G619/H601/H602/public-domain remain separate blockers.
125. [done-prod:g682-screenshot-feedback-loop] Add and production-deploy the internal beta feedback loop requested before wider handoff. The feedback button is visible on logged-in desktop/tablet layouts (`md` and up) and hidden on mobile/small tablet to preserve G636 no-overlap behavior; it captures a viewport-limited DOM screenshot with `html2canvas`, supports preview/retake, sends feedback through deployed `submit-feedback`, stores private screenshots in `feedback-screenshots`, stores rows in `feedback_submissions`, and gives admins a Dashboard `フィードバック` tab with screenshot detail, safe URL links, admin note, and `未対応` / `対応中` / `完了` tracking. Production migrations were applied to Supabase project `ghwjymozrwmcrpjqvbmo`: `20260702100000_beta_feedback_submissions.sql` creates the table/bucket/RLS, and `20260702112251_revoke_direct_feedback_insert.sql` closes authenticated direct INSERT so writes go through the stream-limited Edge Function service-role path only. `submit-feedback` was deployed with stream-based request-size limiting, pre-decode PNG size checks, service-role screenshot upload/cleanup, and URL allowlist normalization. Proofs: local runtime `output/playwright/g682-feedback-runtime-local-r14/SUMMARY.json` `ok=true`; production readback `output/playwright/g682-prod-feedback-readback-r9/summary.json` `ok=true` for authenticated submit, malicious URL normalization, DB row readback, direct authenticated INSERT denial, private Storage object readback, signed URL creation, and cleanup; residual DB readback returned 0 feedback rows/storage objects/temp users; production mass-market QA `output/playwright/g682-prod-feedback-capture-mass-market-r2/SUMMARY.json` `ok=true failed=[]`; production monitor `output/playwright/production-monitor-post-g682-feedback-r2/summary.json` `ok=true`, blockers `0`, `uiOk=true`. Checks passed: typecheck, lint, build, diff check, Deno check, `bash scripts/supabase-prod-verify.sh`, and `npm run verify:internal-ux -- --out output/playwright/g682-feedback-capture-internal-ux-r12`. Read-only Codex review after the mobile fix found no high/medium residual risk. Live chunked oversized-payload probe was aborted and is not acceptance evidence; oversize protection is covered by stream-limit code/static checks. This is a社内beta feedback collection feature only; it does not close G617/G619/H601/H602/public-domain, billing, checkout, payment, or public-launch readiness.
126. [done-local:g686-all-surface-beta-quality-hardening] Completed the additional "未確認も全て" pass for G686 as local/internal-beta hardening. Implemented the preferred logged-in Dashboard/Lightchain visual direction across Dashboard, Lightchain category hub, Fitting, Generate simplification, Brand Settings, and cutout feedback. Production Supabase DB/function support for feedback `cutout` type was applied and read back, but the local UI changes were not deployed to Zeabur and production UI readiness is not claimed. Proofs: `output/playwright/g686-internal-ux-quality-r5/summary.json`, `output/playwright/g686-local-mass-market-full-r3/SUMMARY.json`, `output/playwright/g686-local-lightchain-clone-layout-r4/SUMMARY.json`, visible Playwright CLI screenshots in `output/playwright/g686-visible-ui-r2/`, and `npm run supabase:verify`. Checks passed: typecheck, lint, build, diff check, local internal UX, local mass-market QA, local Lightchain clone layout, Supabase production readback, and read-only Codex reviews. Remaining blockers stay separate: G617 strict same-run, G619 real beta evidence, H601/H602 human decisions, public-domain, billing, checkout, payment, and public launch.
127. [queued:g686-production-ui-readback-after-deploy] After an intentional commit/push/deploy, rerun production mass-market QA and production monitor against `https://heavy-chain.zeabur.app`, then update `STATE.md` with production UI proof. Do not treat the local G686 proof as public-launch completion.
128. [done-local:g687-predeploy-blocker-hardening] Ran the requested "do everything, skip blockers" pass. The normal GitHub push path cannot include `public/models/isnet-general-use.onnx` because it is ~170MB, so the model directory is gitignored and production deploy is guarded by required `VITE_REMBG_MODEL_BASE_URL` wiring in source, Zeabur template, env examples, env check, README, and deployment checklist. Added `npm run verify:rembg-model-deploy`, which passed and verifies no large model is tracked, the env/docs/deploy wiring exists, and missing model load remains fail-closed. Passed checks: typecheck, lint, build, diff check, internal UX r2, Supabase static verification, H601 guard, H602 billing readiness, and rembg deploy-readiness. Skipped/blocked: production UI deploy/readback until the CORS-enabled model URL is configured in Zeabur build env or the model is injected into deploy artifacts; G619 remains blocked by missing real consented beta sessions; G617 strict same-run all-10 remains cost/acceptance-gated; H601/H602 final decisions and public-domain remain human/operator blockers. Local mass-market QA r2 timed out after preview restart and is not acceptance proof.
129. [queued:g687-model-cdn-and-production-deploy] Configure a CORS-enabled model host for `isnet-general-use.onnx`, set `VITE_REMBG_MODEL_BASE_URL` in Zeabur before build, then commit/push/deploy and rerun production mass-market QA, production monitor, and a focused `/fitting` model-fetch proof. Do not deploy the G686/G687 UI bundle without this model URL or an equivalent deploy-artifact injection.
130. [done-local:g687-unverified-followup] Closed the remaining locally actionable unverified items. Local preview on `127.0.0.1:4189` served the ONNX model with HTTP 200 and `Content-Length: 178648008`; local mass-market QA passed at `output/playwright/g687-local-mass-market-predeploy-r3/SUMMARY.json` with `ok=true failed=[]`. Visible Playwright CLI verified `/fitting#fitting-material-workbench` upload, high-precision model fetch (`/models/isnet-general-use.onnx` 200 and ONNX WASM 200), visible `AI切り抜き中`, quality rejection for the difficult white-shirt screenshot input, and `AI生成` remaining disabled after rights confirmation. Screenshot: `output/playwright/g687-visible-fitting-model-fetch-r1/fitting-model-fetch-quality-reject.png`. Current production monitor is healthy when checking the actually deployed asset: `output/playwright/production-monitor-g687-current-prod-asset-current-r2/summary.json` has `ok=true`, blockers 0, `uiOk=true`; the first monitor failure was expected because it compared production asset `assets/index.CCUCIR_w.js` to undeployed local asset `assets/index.DJfEcnC_.js`.
131. [human-needed:g687-hearing-before-production] Need user/operator input before the remaining blockers can be completed: (1) CORS-enabled public/model-host base URL for `isnet-general-use.onnx` or approval for deploy artifact injection, (2) confirmation that this URL can be set as Zeabur build env `VITE_REMBG_MODEL_BASE_URL`, (3) consented real beta session artifacts for G619, (4) H601 legal/commercial-use final decision, (5) H602 pricing/quota/refund/checkout/support final decision, and (6) whether to spend on strict same-run G617 all-10 proof if the current split-run OpenAI mini proof is not enough.
132. [done-local:g688-chosen-public-entrypoint] User chose `https://heavy-chain.zeabur.app` as the official public URL. Created `output/playwright/prod-chosen-public-entrypoint-readback-20260703-r1/summary.json` with `ok=true`, HTTP 200, Heavy Chain shell detected, and submit/payment/publish untouched. `verify-release-gate-unified` and `verify-10m-completion-audit` now validate this chosen public entrypoint proof instead of the old unreachable `heavy-chain.com` custom-domain artifact. Proof: `output/playwright/g688-chosen-public-entrypoint-10m-incomplete-r1/summary.json` no longer lists a public-domain blocker; `output/playwright/g688-chosen-public-entrypoint-release-gate-r1/summary.json` no longer fails chosen public entrypoint readback.
133. [queued:g619-real-beta-session-collection] User confirmed beta collaborators can be gathered. Next step is to collect consented real sessions using the existing G619 packet, store redacted notes/screenshots/readbacks, then run `npm run verify:g619-beta-evidence`. Availability of collaborators is not acceptance evidence until artifacts exist and the verifier passes.
134. [done-local:g689-ai-fitting-canvas-direct-ux] Implemented the user's follow-up requests: AI fitting can combine an uploaded model reference with the extracted garment reference, model-matrix fails closed on unreadable/failed reference generation, Lightchain cards open the actual work pages directly instead of extra intermediate pages, Lab copy/action is clearer, Brand Settings removed the two unnecessary readiness/Runway blocks, and Canvas now uses the Heavy Chain dark direction with image-first actions for background removal, color, upscale, variants, prompt edit, and generation. Canvas prompt edit now calls the real edit API; edit modal prompt/colorize/variations inputs share local legal-safety guards and API failures show toast errors. Proof: `output/playwright/g689-static-ui-followup-readback-r5/summary.json`; checks passed typecheck, lint, build, diff check, Deno check for `model-matrix`, and final read-only Codex review returned `重大/中程度の問題なし`.
135. [blocked-exact:g689-visible-local-ui-proof] Visible Playwright CLI local proof of the final bundle was attempted against `http://127.0.0.1:4191`, but the available local auth state redirected to `/login` (`local_visible_auth_state_redirected_to_login`). Debug artifacts: `output/playwright/g689-visible-ui-followup-debug/canvas-debug.png`, `output/playwright/g689-visible-ui-followup-debug/body.txt`. Do not claim visible end-to-end UI acceptance until a fresh local auth state or production deployment is available.
136. [queued:g689-production-readback-after-deploy] After intentional commit/push/deploy, run production mass-market QA and monitor against `https://heavy-chain.zeabur.app`, plus a focused AI fitting/Canvas readback if auth and safe test assets are available. This must not perform billing, checkout, payment, identity verification, external publishing, or legal finalization.
137. [done-local:g692-white-background-ai-fitting-fallback] Fixed the reported AI fitting blocker where `isnet-general-use.onnx` 404 prevented a white shirt/light-background garment from being cut out and therefore blocked AI generation. The high-precision cutout path now falls back to a browser local white-background cutout only when the edge-connected background-flood engine produces a real bounded transparent garment result; full-frame/geometric fallback remains rejected. Proof: visible Playwright CLI forced `/models/isnet-general-use.onnx` to 404 on `http://127.0.0.1:4193/fitting#fitting-material-workbench`, uploaded `output/playwright/g684-live-fitting-operation-r1/shirt-input-from-user-screenshot.png`, reached `高精度AI切り抜き済みです`, checked rights, and confirmed `AI生成` enabled without clicking generation. Screenshot: `output/playwright/g692-white-background-cutout-r1/visible-forced-404-after-strict-gate.png`. Checks passed: typecheck, lint, build, `verify:rembg-model-deploy`, `verify:internal-ux`, and diff check; read-only Codex review found and helped fix one fallback-quality gap, and final Codex review found no high/medium issue.
138. [queued:g692-production-deploy-readback] Commit/push the G692 fallback, wait for Zeabur to serve the new bundle on `https://heavy-chain.zeabur.app`, then run a focused production `/fitting` white-background cutout proof plus production monitor/mass-market QA as feasible. Do not click AI generation, billing, checkout, payment, identity verification, external publishing, or legal finalization.
139. [done-prod:g692-remove-time-404-and-white-edge-followup] Fixed the production-only residual G692 failure where `isnet-general-use.onnx` 404 occurs inside `remove()` instead of `newSession()`. The fallback now catches remove-time model download errors, clears the cached rembg session, and uses only the quality-gated flood-cutout fallback. Also fixed the user-reported white shirt/light background screenshot edge case by using bright-neutral border median background estimation, handling black edge-connected screenshot bands, and allowing edge feather alpha to reach 0 so the garment crop is bounded. Local direct readback on the reported white-shirt input passed the fallback gate with `boundsRatio=0.9154`. Pre-deploy checks passed: typecheck, lint, build, rembg deploy verifier, internal UX verifier, diff check, and read-only Codex review (`output/playwright/g692-codex-review-internal-ux-r1/summary.json`, no high/medium issue). Production proof after push `3aa360b` and asset `assets/index.BTtAiT0H.js`: visible `/fitting` proof `output/playwright/g692-prod-white-shirt-fallback-r2/visible-prod-fallback-ready.png`, DOM readback `hasReadyText=true`, `hasOldFailure=false`, `aiButtonIsDisabled=false`, requests show real `/models/isnet-general-use.onnx` 404, console shows fallback during `remove()`, final launch-ops `output/playwright/g692-launch-ops-final-r4/summary.json` ok, final production monitor `output/playwright/production-monitor-post-g692-white-shirt-fallback-r3/summary.json` ok with blockers 0 and uiOk true. AI generation was not clicked; billing, checkout, payment, identity verification, and external publishing were not touched.
140. [done-prod:g693-ai-fitting-model-matrix-generation] Fixed the follow-up production blocker where cutout succeeded but `/fitting` `AI生成` failed with a generic non-2xx Edge Function error. `model-matrix` now uses OpenAI image edit for garment/model reference fitting generation, OpenAI text generation for no-reference fallback only, stores `provider=openai` metadata, reserves usage per matrix cell, removes the stale Runway MCP approval requirement from this path, and the frontend now reports the Edge Function JSON error body. Reference image fetch is guarded to data URLs or same Supabase project http(s) URLs, rejects redirects/local/private hosts, requires image MIME types, and caps streamed bodies at 8MiB. Production smoke on `https://heavy-chain.zeabur.app` passed: one authenticated 1x1 fitting generation completed, one generated image row was inserted with `model_used=gpt-image-1-mini`, Storage download read back `1877165` bytes, and the test row/job/file were deleted afterward. Proof: `output/playwright/g693-fitting-model-matrix-openai-r1/summary.json`; earlier compatibility blocker proof retained at `output/playwright/g693-fitting-model-matrix-openai-r1/summary-r1-failed-input-fidelity.json`. Checks passed: typecheck, Deno check for OpenAI/model-matrix Edge code, lint, build, H601 legal-safety verifier, and diff check. No billing, checkout, payment, identity verification, CAPTCHA/OTP, external publishing, or legal finalization was performed.
141. [done-local:g694-h602-brand-settings-quota-copy] Reran post-G693 gates and fixed the only actionable H602 static blocker: `brand_settings_quota_copy_updated`. Added read-only quota/sandbox explanatory copy to Brand Settings; this is UI copy only and does not enable billing, checkout, payment, purchase, identity verification, or external publishing. Proof/checks: `npm run verify:h602-billing` ok, typecheck ok, lint ok, build ok, diff check ok, read-only Codex review ok. Completion audit remains intentionally incomplete with exact blockers for G617/G619/G669/G670/G674 acceptance, H601/H602 human items, G617 strict same-run proof, G619 real beta evidence, production H602 completion readback, G619 verifier, and release gate. Proof locators: `output/playwright/g694-post-g693-10m-completion-incomplete-r1/summary.json`, `output/playwright/g694-g619-beta-evidence-current-r1/summary.json`.
142. [queued:g694-g619-real-beta-evidence] Collect consented real beta artifacts, create the required manifest, and rerun `npm run verify:g619-beta-evidence` until it returns `ok=true`; current blocker is `check_failed:manifest_exists_and_is_parseable`.
143. [queued:g694-release-after-human-items] After G617/G619/H601/H602 are actually closed by evidence/human decisions, rerun release gate and strict 10M completion audit; do not call public launch ready before both are `ok=true`.
144. [done-local:g695-launch-ops-current-auth-and-ui-proof] Fixed the current production launch-ops verifier drift rather than product UI. The verifier now prefers the current QA auth state, requires the real `/generate?feature=campaign-image` detail form, fills `ベースコンセプト` and `タイトル` without clicking generation, and checks the current mobile Generate text. Proof/checks: `output/playwright/g695-launch-ops-standalone-r7/summary.json` ok, `output/playwright/g695-post-g694-prod-mass-market-r2/SUMMARY.json` ok, `output/playwright/production-monitor-post-g694-r2/summary.json` ok, `output/playwright/g695-internal-ux-r1/summary.json` ok, typecheck ok, lint ok, build ok, node check ok, diff check ok, and read-only Codex review ok after removing a false-pass fallback. No billing, checkout, payment, identity verification, external publishing, legal finalization, or generation submit was performed in this verifier slice.
145. [blocked-known:g695-release-and-10m] Broader release/10M remains intentionally incomplete. `output/playwright/g695-post-g694-10m-completion-incomplete-r1/summary.json` still records G617/G619/G669/G670/G674 acceptance blockers, H601/H602 human items, G617 strict same-run proof, G619 real beta evidence, production H602 billing completion readback, G619 verifier, and release gate. Release gate also still rejects `--allow-dirty` as non-acceptance and has historical readback freshness items. These are not caused by the G695 launch-ops verifier drift.
146. [done-local:g696-release-gate-g695-wiring] Updated `scripts/verify-release-gate-unified.mjs` to read the current G695 production monitor, launch-ops, and mass-market QA artifacts instead of older G668/G623 proofs, and aligned the mass-market assertion names with the current simplified UI design. This keeps H602 fail-closed and does not touch G617/G619 completion. Proof/checks: `node --check scripts/verify-release-gate-unified.mjs` ok, `git diff --check` ok, read-only Codex review ok, and `output/playwright/g696-release-gate-current-clean-r3` now clears the G695 monitor/launch/mass-market blockers. Remaining release-gate blockers are expected stale older readbacks for Lightchain/G610/G603/G605/G606/G608/G620/H601, H602 billing completion readback, and dirty tree before commit.
147. [queued:g696-refresh-older-release-readbacks] Refresh the remaining stale release-gate readbacks that do not require human/payment actions: production Lightchain all-feature order previews, G610, G603, G605, G606, G608, G620, and production H601 rights readback. Keep H602 billing completion, G619 real beta, and G617 strict same-run as separate human/external blockers.
148. [done-prod:g697-fitting-dataurl-legal-safety-generation] Fixed and production-deployed the reported `/fitting` path where cutout could succeed but `AI生成` failed with a non-2xx Edge error. The shared Edge H601 guard now ignores only real image data payloads with image magic headers under image-payload keys, keeps `blob:`/`http(s):` URL text inspectable, detects malicious prompt/note/deep metadata, and `model-matrix` passes `imageUrl` to Edge legal-safety validation. Deployed generation Edge Functions to Supabase production project `ghwjymozrwmcrpjqvbmo`. Production authenticated 1x1 `model-matrix` smoke passed with completed job, generated image row, Storage download `2235439` bytes, latest Edge log `succeeded`, and full cleanup of smoke rows/storage. Proof: `output/playwright/g697-fitting-model-matrix-legal-safety-prod-r1/summary.json`; production monitor proof: `output/playwright/g697-production-monitor-post-fitting-fix-r1/summary.json` ok. Checks passed: Deno check, H601 verifier, typecheck, lint, build, diff check, and read-only Codex review. Mass-market QA screenshots were generated under `output/playwright/g697-prod-mass-market-post-fitting-fix-r1/`, including `fitting.png`, but summary generation hung and the verifier was stopped, so that folder is visual diagnostic only, not acceptance proof.
149. [done-prod:g697-repeat-mass-market-qa-summary] Re-ran production mass-market QA after G697 with route watchdog/screenshot timeout added to prevent no-SUMMARY hangs. Formal proof kept `recordVideo=true`: `output/playwright/g697-prod-mass-market-post-fitting-fix-r2/SUMMARY.json` is `ok=true`, `failed=[]`, 17 desktop routes, 9 mobile routes, 26 videos, zero console/page/request failures, and cleanup closed. Post-QA production monitor `output/playwright/g697-production-monitor-post-mass-market-r1/summary.json` is `ok=true`, blockers 0, generation failure rate 0, stale active jobs 0, storage errors 0, and `uiOk=true`; warning remains only old local Runway inbox stale files. No billing, checkout, payment, identity verification, CAPTCHA/OTP, external publishing, legal finalization, or generation submit was performed. Keep G617 strict same-run, G619 real beta evidence, H601/H602 human decisions, billing/checkout/payment, and public-launch readiness separate.
150. [partial-prod:g698-fitting-brand-resolution-visible-proof] Fixed the production `/fitting` brand-resolution regression that made `AI生成` use an inaccessible stale brand after login. The auth store, brand switcher, and Dashboard now resolve only the current user's owned/joined brands; the focused production visible Playwright CLI run sent `brandId=e5571b0b-7af7-4265-9d47-7d90ae4767d3` for the QA account's NiSEN brand, not the stale inaccessible brand. Proof/checks: commit `ea298b9`, local brand REST readback `output/playwright/g698-local-brand-rest-readback-r3/summary.json`, visible E2E `output/playwright/g698-visible-fitting-end-to-end-r4-safe-garment/summary.json`, screenshots `01-dashboard-after-login.png` through `06-after-generate.png`, and video `videos/page@60fca764ced37882e2420e2fb64aba2b.webm`. Static gates before push passed: typecheck, lint, build, diff check, H601 guard, and read-only Codex review.
151. [blocked-exact:g698-fitting-output-quality-final-proof] The same visible production E2E still cannot accept final AI fitting quality because `model-matrix` returns `User usage rate limit exceeded` before job/image creation. No output image was generated, no Gallery/Storage/DB output could be inspected, and therefore no professional-quality handoff should claim the fitting generation result is OK. Exact blocker: `production_ai_generation_blocked_by_free_plan_usage_rate_limit`.
152. [human-needed:g698-internal-qa-quota-decision] To finish the user's requested visible recorded proof, an operator must either approve a temporary non-checkout internal QA quota bypass for this QA account/brand or provide usable generation quota. Do not mutate billing/test-account/quota state automatically; do not perform checkout, payment, purchase, identity verification, CAPTCHA/OTP, or legal finalization. After approval/quota, rerun visible Playwright CLI from login -> upload -> cutout -> rights -> AI generation -> DB/Storage/Gallery readback -> downloaded image visual quality review, then update STATE/plan with accepted or rejected output-quality evidence.
153. [done-local:g699-g606-performance-fixture-refresh] Fixed the current G606 performance verifier drift caused by the auth store's accessible-brand read path now querying `brand_members`. The G606 local fixture now returns read-only `brand_members=[]` and still blocks non-GET/HEAD writes with 405. Proof: `output/playwright/g699-g606-performance-r3/summary.json` `ok=true`, Gallery initial tiles `60`, Canvas objects `180`, export PNG valid, no console/page/request issues, cleanup closed. Checks passed: `node --check scripts/measure-g606-performance.mjs`, `npm run verify:g606-performance` with `G606_OUT_DIR=output/playwright/g699-g606-performance-r3`, and read-only Codex review `no findings`.
154. [partial-local:g699-release-gate-current-readback-wiring] Updated `scripts/verify-release-gate-unified.mjs` to read current G699/G698 evidence where available: mass-market QA, Lightchain all-feature workflows, G610, G603, G605, G606, current production monitor, current G608, current G618, and current G620. This intentionally does not make release gate green; it removes stale green/stale red ambiguity. Proof: `output/playwright/g699-release-gate-current-readbacks-r2/summary.json` fails on current production monitor, G608, G618, G620, stale H601 dedicated rights readback, H602 completion, and `--allow-dirty` / `--skip-commands` non-acceptance blockers. Lightchain validation now counts 31 order-sheet previews because the two AI-fitting handoff features are verified by fitting-handoff assertions, not order-sheet previews; 33 feature results and zero failures are still required. Review: read-only Codex `no findings`.
155. [blocked-exact:g699-production-monitor-and-scale-ops-red] G618 rerun reached the current production monitor readback and failed there, not on G606. Current monitor blockers are recent generation failures/edge or usage failures from the G698/G699 QA window; do not call launch/release ready until a fresh production monitor passes without hiding those rows. Proof: `output/playwright/g699-g618-scale-ops-r2/summary.json` and nested `production-monitor-readback/summary.json`.
156. [done-local:g700-ai-fitting-cutout-generation-hardening] Fixed the current AI fitting reliability issues reported by the user: white/light-background cutout no longer hangs in Playwright/WebGL-unavailable environments, rembg model/session/backend/timeouts fall back to the quality-gated white-background cutout, generated references are normalized to bounded light-gray JPEG before `model-matrix`, and both `AI生成` plus `再試行` are synchronously locked against duplicate requests. Also corrected G620 static classification so `model-matrix` is treated as OpenAI-backed rather than Runway-backed. Proof/checks: visible Playwright CLI `output/playwright/g700-fitting-reference-normalization-local-r8-double-click/summary.json` ok with one request after repeated AI生成 clicks; visible retry proof `output/playwright/g700-fitting-retry-lock-local-r1/summary.json` ok with forced first failure plus one retry request after repeated retry clicks; `output/playwright/g700-local-mass-market-fitting-normalization-r4/SUMMARY.json` ok; `output/playwright/g700-internal-ux-fitting-reference-normalization-r3/summary.json` ok; typecheck, lint, build, Supabase static verification, diff check, and final read-only Codex review OK. No billing, checkout, payment, identity verification, CAPTCHA/OTP, external publishing, quota bypass, or real production generation submit was performed in G700.
157. [blocked-exact:g700-production-output-quality-and-monitor] G700 does not close production output quality or public launch. Remaining exact blockers: production monitor/G620 still red from recent generation/usage/edge failures in the 96h window, and AI fitting output quality still needs a real visible production generation with downloaded/DB/Storage/Gallery readback and human visual quality review after usable quota or an operator-approved non-checkout internal QA quota path. Keep G617 strict same-run, G619 real beta evidence, H601/H602 human decisions, and launch readiness open.
158. [done-prod:g700-deploy-and-mass-market-readback] Pushed commit `5a0b976` to `origin/main`; production served the expected new `assets/index.CVKdyk1U.js`. Production mass-market QA passed at `output/playwright/g700-prod-mass-market-post-fitting-hardening-r1/SUMMARY.json` with `ok=true failed=[]`. Production monitor after deploy is intentionally not accepted: `output/playwright/g700-production-monitor-post-fitting-hardening-r1/summary.json` has `ok=false`, blockers 2, warnings 3, generationFailureRate 1, storageErrors 0, and `uiOk=true`. Treat this as a current G620/G618/launch blocker until the recent failed generation/usage/edge rows are resolved by an approved operator path.
159. [partial-prod:g701-visible-ai-fitting-verification] Added the visible Playwright CLI verifier for `/fitting` so preflight and real generation are not confused. Preflight passed on production with the reported white/light-background shirt path: upload, high-precision cutout, rights confirmation, and enabled `AI生成` (`output/playwright/g701-visible-fitting-prod-preflight-r1/summary.json`). Real submit was attempted in visible production and failed at `production_ai_generation_blocked_by_usage_rate_limit`, while confirming the request now sends JPEG data (`imageUrlMime=data:image/jpeg`) and rights confirmation; no job/image/storage output was created. Latest proof: `output/playwright/g701-visible-fitting-prod-submit-r2/summary.json`, `05-after-generate.png`, and recorded video.
160. [done-local:g701-fitting-error-message-japanese] `/fitting` now maps `model-matrix` failure strings through `getErrorMessage`, so raw English errors like `User usage rate limit exceeded` become the existing Japanese rate-limit guidance after deploy. Checks/reviews passed: `node --check scripts/verify-g701-visible-fitting-prod-e2e.mjs`, typecheck, lint, build, diff check, and read-only Codex reviews for the verifier and Fitting error mapping.
161. [blocked-exact:g701-final-output-quality] AI fitting output quality is still not accepted. The remaining blocker is usable generation quota or an operator-approved non-checkout internal QA quota path, followed by a fresh visible production run through upload -> cutout -> rights -> AI generation -> DB/Storage/Gallery/download -> visual quality review. Do not perform billing, checkout, payment, purchase, identity verification, CAPTCHA/OTP, external publishing, destructive cleanup, or quota bypass automatically.
162. [done-prod:g701-deploy-japanese-error-readback] Pushed commit `626ce05` to `origin/main`; production launch-ops rerun `output/playwright/g701-launch-ops-post-push-r2/summary.json` passed after deploy and confirmed current asset `assets/index.DvwKW5i0.js`. Visible post-deploy `/fitting` proof `output/playwright/g701-visible-fitting-prod-submit-post-deploy-r3/summary.json` reproduced the same real quota/rate-limit blocker while recording `hasJapaneseRateLimitGuidance=true` and showing the Japanese user-facing message in `05-after-generate.png`: `短時間に生成リクエストが集中しています。1分ほど待ってから再試行してください。` The verifier now refuses `ok=true` until explicit visual quality acceptance is provided after technical DB/Storage/download success, requires `visualReviewer` and `visualReviewArtifact` for that acceptance, and sanitizes non-JSON response previews.
163. [blocked-exact:g701-production-monitor-g618-g620] Post-deploy monitor `output/playwright/g701-production-monitor-post-deploy-r1/summary.json` remains red from recent failed generation/Edge/usage rows; UI probe passed, storage errors are 0, stale active jobs are 0. `npm run verify:g618-scale-ops` passed local build/performance fixture but failed at production monitor readback (`output/playwright/g701-g618-scale-ops-r1/summary.json`). `npm run verify:g620-security-ops` also failed at production monitor readback (`output/playwright/g701-g620-security-ops-r1/summary.json`). Do not claim G618/G620/release ready until monitor is clean or an approved operator path resolves the rows.
164. [blocked-exact:g701-g619-release-human-items] `npm run verify:h601-legal-safety` ok, `npm run verify:h602-billing` ok, and `npm run security:audit` ok. `npm run verify:g619-beta-evidence -- --out output/playwright/g701-g619-beta-evidence-current-r1` still fails on `check_failed:manifest_exists_and_is_parseable`. `npm run verify:release-gate -- --out output/playwright/g701-release-gate-post-deploy-r1 --skip-commands --allow-dirty` remains red on production monitor, G618/G620, stale H601/H602 readbacks, G619/human items, and the explicit non-acceptance of skipped commands/dirty mode.
165. [done-prod:g702-release-gate-readback-correction] Re-ran and rewired current release-gate readbacks to avoid stale-proof misreads. Current proof paths: production monitor `output/playwright/g702-production-monitor-refresh-r1/summary.json`, G608 `output/playwright/g702-g608-security-audit-r1/audit-readiness.json`, G618 `output/playwright/g702-g618-scale-ops-r1/summary.json`, G620 `output/playwright/g702-g620-security-ops-r1/summary.json`, H601 `output/playwright/g702-prod-h601-rights-check-r1/summary.json`, H602 static/readiness `output/playwright/g702-h602-billing-readiness-r1/summary.json`, and clean-tree release gate `output/playwright/g702-release-gate-current-clean-r1`. H601 is now fresh and no longer a release-gate blocker; H602 readiness now writes its `--out` artifact instead of only printing JSON.
166. [blocked-exact:g702-current-release-blockers] `output/playwright/g702-release-gate-current-clean-r1` still fails on production monitor, G608 security audit, G618 scale ops, G620 security ops, and H602 production billing completion. The monitor has real recent generation/edge/usage failures; G618/G620 fail through that readback. G608 current audit fails `runway_bridge_tools` and `approved_live_generation_readback`. H602 readiness verifier is ok, but H602 completion remains human/operator gated and no checkout/payment/purchase was performed.
167. [done-local:g703-g608-local-worker-wording-fix] Fixed the G608 audit false positive where `runway_bridge_tools` required the retired STATE literal `local Runway MCP worker queue` even though the current source of truth uses local-worker/import/watch wording. Proof `output/playwright/g703-g608-security-audit-r1/audit-readiness.json` now has `summary.passed=10`, `failed=1`; only `approved_live_generation_readback` remains failed. Release gate now reads this G703 G608 proof.
168. [blocked-exact:g703-current-release-blockers] Clean release-gate proof `output/playwright/g703-release-gate-current-clean-r1` still fails on current production monitor, G608, G618, G620, and production H602 billing completion. G608 is now a real live-generation-readback blocker, not a Runway-path wording blocker; `git_dirty` / `allow-dirty` is no longer a blocker in this clean proof. Next safe work is either wait/approved-operator resolution for production monitor recent failures, collect G619 consented beta manifest, or obtain approved non-checkout generation quota for visible AI fitting output quality review. Do not perform billing, checkout, payment, purchase, identity verification, CAPTCHA/OTP, external publishing, destructive cleanup, quota bypass, or generation submit automatically.
169. [blocked-exact:g704-production-monitor-refresh] Reran production monitor at `output/playwright/g704-production-monitor-refresh-r1/summary.json`; it is still red with `generation_failure_rate_high` and `recent_generation_jobs_failed`, while `uiOk=true`, stale active jobs are 0, and storage errors are 0. Release gate now reads the newer G704 monitor proof; clean proof `output/playwright/g704-release-gate-current-clean-r2` fails only on production monitor, G608, G618, G620, and H602 billing completion, with no dirty-worktree blocker. G618/G620 were not rerun because Codex investigation confirmed they would fail through the same red monitor; rerun them only after monitor passes or approved operator action resolves the recent failed rows.
170. [done-prod:g706-gallery-loading-actionable-prod] Gallery loading is no longer a dead-end during production monitor/readback. Follow-up commit `5bdd0bd` makes the loading panel show explicit `再読み込み` and `新しく生成` actions immediately, and the launch-ops verifier accepts only loaded images, actionable loading controls, or a settled empty fallback. Zeabur served expected `assets/index.EBeBUMvf.js`; post-deploy monitor proof `output/playwright/g706-production-monitor-post-deploy-r6/summary.json` still has `ok=false`, but `uiOk=true` and the Gallery check passed with `explicitLoadingActions=true` and `fallbackVisible=true`.
171. [blocked-exact:g706-remaining-monitor-and-release] Release/public readiness remains blocked after the Gallery fix. Current monitor blockers are recent generation/job failures plus Edge/usage warnings (`generationFailureRate=1`, blockers 2, warnings 3), not a Gallery UI failure. Next safe candidates remain: wait for the monitor window to clear or receive approved non-destructive operator handling, collect G619 real beta evidence, finish H602 human/operator completion, and rerun release/10M gates only after those blockers are closed. Do not perform billing, checkout, payment, purchase, identity verification, CAPTCHA/OTP, external publishing, destructive cleanup, quota bypass, or unnecessary generation submit automatically.
172. [partial-prod:g707-release-gate-monitor-refresh] Refreshed production monitor again without submit/payment/cleanup at `output/playwright/g707-production-monitor-refresh-r1/summary.json`; it still has `ok=false`, blockers `generation_failure_rate_high` and `recent_generation_jobs_failed`, warnings for Edge/usage/local inbox, `uiOk=true`, stale active jobs `0`, and storage errors `0`. Updated `scripts/verify-release-gate-unified.mjs` to read this newest G707 monitor instead of G704. Codex read-only review found no weakening: monitor `ok=false` remains a release-gate failure, and H602/G617/G619 are not hidden.
173. [blocked-exact:g707-current-release-gate] Current release gate proof `output/playwright/g707-release-gate-current-r2` remains red on production monitor, G608 security audit, G618 scale ops, G620 security operations, production H602 billing completion, plus temporary `git_dirty` before this commit. The actionable non-human next step is to rerun release gate after committing this path/doc refresh to remove only `git_dirty`; the real blockers should remain until the monitor window clears or approved operator handling/human evidence closes G608/G618/G620/H602/G619/G617.
