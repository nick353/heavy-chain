# G404 Production Closeout

## Goal

Lightchain上位互換化の修正を統合し、localとZeabur productionで録画・スクショ・DOM/readback確認を通し、`STATE.md` と `plan.md` を更新して push まで完了する。

## Scope

- Static gates and Codex read-only review.
- Local preview and production UI proof.
- Deployment/readback only after checks pass.
- No billing/payment/identity/external publish actions.

## Done Evidence

- `npm run typecheck`, `npm run lint -- --max-warnings=0`, `npm run build`, `git diff --check` pass.
- Codex read-only review passes or findings are fixed.
- Production asset/readback and recorded UI proof show Lightchain-like flow.
- `STATE.md`, `plan.md`, `GOAL.md` point to final proof paths.
- Commit and push to `origin main` succeed.

## Child Goal Text

`/goal Close out the Lightchain-superset clone work in production, verified by static gates, Codex review, local and Zeabur recorded UI proof, state/docs updates, and push, while preserving all safety boundaries. If blocked, report exact failing gate, route, artifact path, and required unblocker.`
