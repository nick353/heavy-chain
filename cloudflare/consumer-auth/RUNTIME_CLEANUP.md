# Heavy browser runtime cleanup — 2026-09-06

最新2026-09-08: Canvas metadata local verifierの`model-custom`停止を現行Cloudflare契約へ修正。可視結果タイトル待機、局所`--only`、local `model-matrix` fixtureの`matrix[]`応答を追加し、CF build/typecheck/node syntaxとmodel-custom readbackがPASS。workspace入口は`canvas_save_button_missing`（provider POST/権利確認/保存結果なし）が継続し、全体verifierは未完。generic生成のauth-brand fence再読込を一度だけ追加したが、追加deploy・実AI・実R2・実機・全通信zeroは未確認。

最新2026-09-08: 旧Edge smokeのactive実行経路をCloudflare runtime contractへ統一。`monitor-production-health.mjs`、`collect-workspace-live-readback.mjs`、`hc-10m-real-generation-qa.mjs`は既にCloudflare authenticated GET/receipt/readback契約であり、旧Edge smoke aliasも同じcontract wrapperになった。旧Supabase/Edge treeを読むactive smokeは残していない。これはlocal/static運用gateの証拠で、実メール・実AI・実R2業務・実機・全通信zero・旧サービス停止の証明ではない。

最新2026-09-07 08:48JST: 認証復旧層をCloudflare専用`auth`/`authSessionRecovery`へ整理し、旧Supabase SDK/URL/env/fallback参照を本番Web sourceから除去。Generate/Canvas/Workbenchの旧OpenAI/Gemini fallbackもWorkers AI fail-closedへ統一。Heavy APIの旧Supabase共有legal-safety importをCloudflare API内へ移設。web test8/8、root typecheck/build、Heavy API typecheck、画像runtime21/21 PASS。Web version `905305df-c150-473d-9eec-08c7681941d1`を100%配置し、health200/authProvider=cloudflare、served JS旧runtime marker0をfresh readback。実認証・実AI・実メール・実機・全通信ゼロ・旧サービス整理は未完。

最新06:51JST: 公開env制限/生成来歴/rootSDK除去候補を通常CF build・関連36test・dry-run後、Web61edc449-d9f8-4d33-b762-3e7e519325e3へ100%配置/readback。consumer-auth/publicasset binding維持、51static更新68再利用、既存大型2asset実GET SHA/size一致で再uploadなし。主要4JS本番SHA一致、health200/session200null、Companion実ログイン画面視認と専用tab/session cleanup確認。101JSの旧runtime設定検索0件は通信ゼロ証明ではない。実登録/本番AI/実機/実通信ゼロ/旧サービス整理・全11工程は未完。API9ce5c95f維持。work/cloudflare-web-env-release-20260907.json参照。

最新G618移植: 運用baseline runnerをCF monitor v2へ接続。明示origin/brand/sessionをbuild前検査し、実件数整合/非空完了job/全sample privatehash/利用量期間と推計を検証。旧Edge・signedURL・既知失敗除外を撤去、空route性能値の偽合格も修正。新4+monitor6/syntax/diff成功、実baseline/build/本番計測は未実行。CPU/同時負荷/請求/fleet SLO/業務完了は未検証。全11工程active、Webfe84242c/API9ce5c95f維持。

最新v2検証: workspace verifierに--expectationsのCF保存証跡整合性検証を追加。明示origin/brand/user/since/job/request/candidate数/protected条件と15分内観測、全工程、最終link/private hashを照合。businessCompletionはnot_verified維持、旧cleanup/Edge判定と分離。新4/collector8・syntax/diff成功、実本番未実行。G618移植・本番実機/実通信ゼロ・全11工程は未完、Webfe84242c/API9ce5c95f維持。

最新SDK整理: 最後の実QAをCF receipt/readback既定・明示submit1回/排他journalへ移植。旧provider/固定identity/自動enqueue/cleanupを除去しroot package+lockからSupabase依存除去。candidate/工程/final/privatebyteを照合しunknown/未合成は未完。QA最終6・前段monitor6/collector8・tsc/help/diff成功、実推論なし。node_modules/cache/旧Deno史料は未削除、実通信ゼロ・本番実機・旧判定器v2化は未完。Webfe84242c/API9ce5c95f、全11工程active。MONITORING.md参照。

最新収集追跡: source job→実ledgerのcompleted最終保存→正確なimage/finaljobのCF GET照合を追加。owner/brand/time/path/source/request/candidate/receipt hash+sizeを再検査しcanonicalFinalLinks記録。中間/申告alias/未保存phaseは追跡・再送しない。collector8+monitor6/diff成功、実本番未実行。旧完了判定器2本のv2化とrealQA/SDK除去、本番実機/実通信ゼロは未完。Webfe84242c/API9ce5c95f、全11工程active。

最新収集移植: workspace:collect-readbackを指定job/brand/since/live sessionのCF GET専用v2へ置換。実工程ledger/正確なowner関係/privatebytes/hashを取得し、入力工程の完了捏造を除去。collector5+monitor6/source契約7/help/syntax/diff成功、実本番未実行。protected source→別finaljobの自動追跡と旧closeout判定器v2化は未完（正確なfinaljob指定が必要）。scripts内SDK参照はrealQA1本、package残存。Webfe84242c/API9ce5c95f、全11工程active。契約MONITORING.md。

最新監視移植: monitor:productionをCF認証GET専用v2へ置換し、このscriptのSDK/旧env/管理キー/旧既定brandを除去。現principalのjobs/mediaとbrand月次推計を区別、private実byte/hash、bounded pagination、空/未確定/失敗を検査。focused6/help/syntax/diff成功、実monitor未実行。旧G618 gateはv2を明示拒否し偽合格防止（gate移植は未完）。旧collector/realQA2本とSDKpackageは残る。本番Webfe84242c/API9ce5c95f、全11工程active。契約cloudflare/heavy-api/MONITORING.md。

最新未配置候補: Fitting/Workbench/MaterialWorkbenchの新規結果来歴を既存CF実行経路へ整合し、旧Supabase/OpenAI既定表記を除去。保存済みFitting履歴は既存metadataを優先、欠落時unknown（CF実行と推定しない）。model-matrix応答backendProviderを保持。関連16/tsc -b/diff check成功。前段env候補も未配置、本番Webfe84242c/API9ce5c95f維持。SDK/package/旧運用script・本番実機/実通信ゼロ・全11工程は未完。

最新未配置候補: Viteの公開env prefixを現行Cloudflare/画像モデル設定に限定し、旧Supabase変数の個別マスクと不要Auth flagをbuildから除去。.env.exampleをCF構成へ更新。実browser/worker fixture buildで旧URL/key/無関係/サーバー専用値の不在と必要設定の保持1/1、hosting8/8、tsc -b/diff check成功。実秘密env未変更、Web本番fe84242c/API9ce5c95fのまま。SDK/package/旧運用script/provider表記・本番業務/実通信ゼロ・全11工程は未完。

## Web release 2026-09-06 23:29JST

Web fe84242c-3403-49b5-9aa6-04d188d1a3a9 created14:27:50.108UTC/deployed14:27:50.572UTC at100%, fresh versions/deployments confirmed. Reused preceding normal CF build and27 passing focused tests/dry-run.51 static assets updated/68 reused; consumer-auth/publicR2 bindings unchanged. Actual public GET matched both unique large assets (wasm26827543bytes/SHA78feeeb3d08f6bcee94d938ed322f69073bb8076b5f9d34697a574ffba8deb48; ONNX44173029bytes/SHA75da6c8d2f8096ec743d071951be73b4a8bc7b3e51d9a6625d63644f90ffeedb). No large R2 upload. Main index.DA3OCaVh.js SHA f79242778595e0f01b0f4b2d9429d64d53a765d52a421372d0ae47bb843ab912, workspaceActivity.Bwuh34S5.js SHA f65a38066aa0728846cc4e2f20c23b9227462747a76a499a18596564b503fcce, auth boundary supabase.BVwcKXm6.js SHA ff14108ddabdca915cdbe2bcd7a37513e45fe03efdbf012117e39a64430f1415 all match served bytes. Old module filename is not an SDK dependency.

Health200/cloudflare and session200/null. Companion task-owned tab1980913130 on current generation showed complete login controls and matching screenshot14:29:01.908UTC; initial loading-only tab1980913127 was automatically closed, not counted as UI success. No login/signup/provider submission; final session close cleans own retained readback tab. This proves public UI only, not authenticated Gallery/Canvas/Jobs/management or full-network-zero. No MyPro, old data/services, settings/permissions or AI invocation in this release. API remains9ce5c95f. All eleven stages remain active.

## Execution API deployed 23:24JST; grouped Web candidate built

API9ce5c95f-4c76-4ba1-a0e9-01ef69745554 created14:24:32.752UTC and deployed14:24:33.355UTC at100%, confirmed by versions view and deployments list. Full API86/86, typecheck and production dry-run184.36KiB/gzip39.54 passed. MEDIA_READ_SECRET, consumer-auth, existing D1/privateR2, image AI false and public share false remain. Health200 and new execution GET without/with invalid bearer401 were read back; no authenticated user workflow, inference, migration or old-data action occurred.

Grouped default Web build completed with CF auth/media, main index.DA3OCaVh.js; hosting/auth/Gallery/management27/27 passed. Web remains b3fcf964, pending asset verification, representative UI QA and deployment. SDK package still serves three old operational scripts: monitor-production-health.mjs (service-role DB/storage/old UI monitoring), collect-workspace-live-readback.mjs (old DB/telemetry/signed URLs), hc-10m-real-generation-qa.mjs (old direct provider/legacy identities). Their package commands are still connected; do not simply uninstall the package or claim these operations migrated. Replace their operational contracts using the existing CF APIs and scoped current identity. No script was executed or modified in this audit. All eleven stages remain active.

最新認証型候補: browserAuthTypes.tsの最小Cloudflare UI契約へ3か所のSDK型importを置換。認証11テスト・最終AuthStore5・tsc -b/diff check成功。src内@supabase/supabase-js参照ゼロ、packageと旧運用script3本の参照は残る。未build/未配置、工程APIも未配置。実通信ゼロ/本番実機・全11工程は未完。

## Execution-record candidate (not deployed)

Removed the last workspaceActivity old lightchain_task_steps query and the unused exported Supabase data Proxy. No source consumer imports the retired data object now; auth entrypoint names and three SDK type imports/package still remain. Old table/type declarations are not copied into Cloudflare or queried. New GET /v1/workspace-execution-steps is scoped to the current viewer principal and brand and up to100 requested job IDs. It projects existing heavy_ai_requests/candidates plus deterministic final-save generation_jobs/image rows, with no schema, inference, reconciliation, R2 or D1 writes. Source jobs and their exact protected final jobs share stable per-candidate step identities; a row merely mentioning a request cannot adopt its steps.

WorkspaceActivity obtains steps for the visible jobs/output lineage, in bounded batches after those reads. Actual AI/intermediate/private/final-save states remain separate, including unknown/not-started; a completed aggregate job or declared task list no longer marks every task complete. Existing local-output/remote-image canonical step association and deduplication remain. Input metadata is labelled 入力工程（申告）; independently read execution rows are labelled 実行記録. Missing/failed optional records do not hide saved work or invent completion. These are persisted execution records, not a fresh guarantee of media availability or AI quality.

Verification: full API86/86 (including2 new real SQLite protected-final/unknown-state tests); client/activity/material43/43; AuthStore5/5; API and frontend typechecks exit0. Step GET tests assert total_changes, inference calls and R2 puts do not increase; cover before/after final save, exact final identity, forged alias, foreign owner, invalid scopes and unknown/planned/failed states. The existing auth test initially expected the deleted Proxy to throw; it now verifies the export and Proxy are absent and all5 pass. Source inventory found no remaining value import/data call on supabase. No production user data, old service or MyPro changes in this slice.

Deployment remains API27f0d340 and Webb3fcf964. This new execution endpoint must be deployed before the pending Web cleanup is released. Remaining SDK type/package/env cleanup, provider label cleanup and authenticated production/browser/device verification remain required; all eleven goal stages are active.

## Server query extension deployed at23:02JST; browser candidate still pending

Added optional feature_type/job_id and validated oldest/newest ordering to the existing owner/brand-scoped generated-images endpoint before LIMIT/OFFSET. Feature/job values are bound parameters, sort direction is selected from literal ASC/DESC, and existing purpose/favorite/has_job/intermediate exclusions remain. Both API package and browser client expose these options. Fitting now requests model-matrix + exact resumeJob with limit1, or model-matrix history with limit100; its two old DB branches/imports are removed. Gallery sends its order control to the API. No migration or old data copy was needed.

Tests: API full84/84; focused Gallery real SQLite and package client7/7 (overlaps full suite), browser/Fitting27/27; both API/frontend typechecks passed. SQLite fixtures include120 newer unrelated rows, same-date tie ordering/offset, foreign user/brand, intermediate rows, combined predicates and malformed/injection-like filters. These are synthetic local data, not real-user end-to-end evidence.

Production API 27f0d340-14a6-488c-a833-ec31d2d2f921 created14:02:34.872UTC/deployed14:02:35.481UTC at100%, confirmed by deployments list + versions view. Dry-run/upload179.23KiB/gzip38.20KiB; startup4ms. consumer-auth binding/issuer, exact D1 f2a6ef3b-14d0-4527-94eb-59ed22a469d9, heavy-chain-private-media, MEDIA_READ_SECRET, AI_IMAGE_ENABLED=false and PUBLIC_SHARE_ENABLED=false remain. Same-run health200/private-r2; query without auth and with an invalid bearer both401. No authenticated production query, real image generation, MyPro change, service cleanup or schema/data mutation was performed.

Web cleanup including Fitting/order remains unbuilt/undeployed; current Heavy Web is b3fcf964. The query gap noted in older sections below is now implemented/deployed server-side, not yet exercised through the new Web candidate. Remaining task-step coverage, provider labels, SDK type/package/env removal, grouped Web build/QA/deploy, actual authenticated traffic and full production/device requirements remain active.

## Next data-path candidate (not deployed)

Gallery/reuse extension: Gallery, Dashboard, GallerySelector, LightchainLibrary and LightchainWorkbench now use existing CF image/folder/membership/favorite/delete contracts without legacy DB/storage branches. Selector's purpose/favorite/job predicates still run before API pagination; private signing, local-first results, canonical identity and Library-to-Canvas handoff remain. Workbench no longer restores raw/expired URLs after an unexpected signing rejection. Dashboard brand creation uses the existing same-session user/access/readback checks, selects the confirmed new brand before checking selection (the prior CF-only branch incorrectly required it to be already selected), and explicitly says not to recreate a confirmed-created brand.

Verification: Gallery client/filter/mutation/source, local-first/download, Library handoff and Dashboard checks14/14 passed; API owner-scoped images/private R2 content-and-delete checks2/2 passed; final frontend tsc-b exited0. API deletion source confirms owner check, R2 delete then D1 row delete; no actual user images were deleted in this slice. Tests use synthetic transport or backend doubles, not real browser/prod users. Production remains b3fcf964; no new build/deploy.

Next fidelity gap: Fitting still has old read branches and its current CF path filters feature/job only after a100-row fetch. Add scoped server-side feature/job predicates before removing those branches; also support Gallery oldest ordering before pagination (currently only client sorting of newest100). Lightchain task-step source and stale provider labels remain. Bulk delete partial-failure UI semantics, whole-app authenticated traffic and actual production/device flows are not proven by these local checks. All eleven stages remain active.

Management UI extension: removed old DB/storage branches from TagManager, StylePresets, TeamManagement and BrandSettingsPage. Tag selection rollback and preset server readback remain. BrandSettings member reads/invites/removal/role updates now use the existing Cloudflare API; member actions and React keys use user_id (the CF contract does not return legacy membership id). Invite code/expiry are server-owned, not Math.random/browser-generated. A failed clipboard write after successful invitation creation is reported as copy failure, not creation failure. No real invitations, role changes, old-data deletion or storage migration occurred.

Verification: new management client/source suite3/3 covers19 actual adapter operations, scoped paths/body/response identity and permission failures without retry/fallback. Existing API core folder/tag/preset/team tests4/4 pass (test doubles, not production users). Initial frontend fixture lacked window; after adding the same synthetic browser origin used by existing auth fixtures it passed3/3. Final post-clipboard npx tsc-b exited0. No new Web build/deploy or real browser management workflow yet. Remaining runtime callers are tracked below and in source; do not treat these local checks as full migration proof.

Workspace usage/folders extension: FolderManager now calls only the existing Cloudflare CRUD methods and rejects missing API config (previous slice typecheck passed). WorkspaceActivity now reads Cloudflare imageUsage, generation jobs and generated images only for those three sources. The old usage RPC and Jobs/image table fallback branches were removed. Unknown/invalid quota responses remain unavailable with null values, never Free25. Completed, running and uncertain candidate counts are distinct; SQL empty-month null counts normalize to zero only after valid quota readback. The progress meter uses allocated quota (monthly minus remaining), not completed-plus-running images, and explicitly disclaims account-wide free balance or invoice meaning. Credits offers reload when quota is unavailable; Dashboard/shared panel show unknown without hiding saved Jobs/History.

Verification: actual Vite client and React rendered markup fixtures6/6 plus existing activity routing13/13 passed together19/19, final npx tsc-b exited0. Covers non-default server quota, failed/malformed usage, empty-month sums, one 401 refresh/read retry, preserved completed Jobs, and rendered unknown/allocated percentage. The first render test failed because the test directly SSR-loaded a CommonJS dependency; ordinary dependency import fixed the fixture, then all19 passed. Fixtures use synthetic sessions, no production AI/data writes. No build or deployment was done; production remains b3fcf964.

Remaining exact source boundary: fetchLightchainTaskSteps still references the old optional table; no Cloudflare task-step endpoint/table was found in the current API inventory. Do not silently replace it with empty success or claim per-step completion from aggregate job status. Preserve source context/lineage and connect real persisted step information before removing that dependency. Other UI legacy branches and SDK type/package removal remain pending. All eleven goal stages remain active; old data/services were untouched.

Storage/artifact extension: removed Supabase signing, batch salvage and provider-fallback execution from storage/mediaGateway; runtime default is Cloudflare-only, retired/malformed provider settings disable gateway access. Removed the legacy workspace-artifact function/error-parser/timeout wrapper; existing CF transport timeout, stable request ID, local error record and readback remain. No legacy account/data cleanup was performed. Actual workspace/storage tests plus path-safety checks passed17/17, updated CF gateway checks6/6, final tsc-b exited0. One obsolete source assertion expected the deleted Supabase batching code; it was replaced with bounded-CF assertions and actual 4-worker/dedup/partial-failure checks. The unused multi-provider resolver and its obsolete fallback tests were removed, retaining invalid-path-before-credentials coverage. This extension is unbuilt/undeployed; production remains b3fcf964. Remaining UI/workspace-activity callers, SDK types/package and whole-app traffic proof are still pending.

Removed legacy function/URL/key branches from imageApi, CanvasEditorPage and GeneratePage; Canvas document persistence now delegates only to the existing Cloudflare get/create/update methods. Missing API config rejects before edit-input fetch. Existing image/context/idempotency options, Canvas document ID/expected revision and no wrapper-level write retry are retained. A missing Canvas provider label is now explicitly cloudflare-provider-unreported instead of claiming Supabase.

Focused image/share/Canvas adapter and existing Canvas source tests passed11/11; earlier image normalization check also passed. Final tsc-b exited0. Actual transport fixtures check authenticated share creation/public credential-free reads/invalid-token rejection, and Canvas context/ID/revision/error propagation. No real AI inference, public sharing or production writes occurred. Production remains `b3fcf964-7dd5-4ef0-9c2a-18e81e121d14`; no new Web build/deployment for this candidate. Remaining storage/localWorkspaceArtifacts and other legacy callers still need cleanup before the next grouped build.

## Implemented candidate

- `src/lib/supabase.ts` no longer imports/constructs the Supabase SDK at runtime. It always uses the existing same-origin Cloudflare auth client; missing/stale flags cannot restore the old engine. Legacy URL/key lookup, auth lock and password-reset/probe fallback are removed from this entrypoint.
- Auth-store profile/brand reads and profile updates use the dedicated Cloudflare API only. Legacy table reads/upserts/updates, optional owner-only fallback and legacy token-storage deletion are removed. Pending signup does not create/adopt an API profile or a session. The old persisted account data is not read, copied or erased.
- Both profile mutation and initial profile hydration reject late commits after an account change or a newer admission of the same account, using the existing brand request generation. Server writes may have succeeded before an old response is rejected; these checks protect UI state and do not authorize replay.
- Default `node cloudflare/heavy-web/build.mjs` now always emits the consumer-auth service binding and Cloudflare client flags. It no longer loads/accepts the temporary Supabase auth bridge. Empty overrides still mask stale local Supabase env until remaining source callers are removed.

## Verification and limits

Actual auth-store Vite SSR tests passed5/5 (including same-account delayed hydration); existing browser-auth transport tests passed6/6 earlier in this slice. Final default build/typecheck exited0 (2552 transformed modules), dry-run4.03KiB/gzip1.49 passed. Tests use explicit synthetic auth/API responses, not provider or production users.

Deployed Heavy Web `b3fcf964-7dd5-4ef0-9c2a-18e81e121d14`, created13:17:23.478UTC/deployed13:17:24.176UTC,100% readback.49 changed static assets/71 reused, existing large manifest unchanged/no large R2 upload. Auth binding remains consumer-auth, public R2 only. Health200/cloudflare/session200null; served index.CtXUJLCS.js SHA `ea2981c2b9e6ff03f461d5cdea1085f6b10aba61309f5fa1e5dbba9b41349c7c` and named legacy-boundary chunk supabase.DLvxJgoE.js SHA `387e90db396637b805b60ebedd8e749c3d618bb357bdc5c3fd35ce494f50af72` match the local build. That chunk name refers to the existing file, not an SDK client. No authenticated production browser flow was performed in this slice.

This is an incremental removal, not completion of goal10. Three source imports remain **type-only** (`SupabaseClient`/Session/User/AuthChangeEvent), and the package is still installed. The old data-call proxy throws rather than creating a network client; it must be deleted after remaining callers are migrated/removed, not retained as the final design. Legacy branches remain in image/storage/share-link, workspace/canvas persistence, Gallery/brands/team/tags/presets and other pages. Media reference/provider configuration and native MyPro dependencies still require their own source/traffic audit. No real authenticated traffic proof or old-service cleanup occurred.
最新2026-09-07 22:45JST: Lightchain Canvas metadata verifierの現行UI移行候補を更新。fabric/printingのfile input束縛を現行selectorへ限定し、local proofでfabric 5/5、printing 4/4、line-to-real/line-generation/pattern-vector-pro/svg-convertも通過、外部生成request 0。marketing-detailの旧alt text待ちtimeoutとconsole 404で全体は未完。製品runtime・backend・deploy・実AIは変更/実行していない。
最新2026-09-07: Canvas metadata verifierの現行UI移行を継続。marketing-detail 3/3成功、print-design-detailは可視enabledボタンのクリック後も現行`lightchain-print-design-readback`待ちでtimeout。fabric/printing/line系/svgの既存成功と外部生成0は維持。これはverifier-only local proofで、製品runtime/backend/deploy/実AIは未変更。
最新2026-09-07: `print-design-detail`の追加local診断でも、可視・enabledボタンのクリック後10秒でreadback未生成、UI generation errorなし。provider失敗とは断定せず、現行UI handler/state更新境界のexact blockerとして保持。runtime/backend/deploy/実AIは未変更。
最新2026-09-07 23:15JST: Heavy Web Cloudflare-only候補を通常build/CF build/dry-run後、version `6e4cae93-caeb-457e-acaa-ef18b03b356a`へ100%配置。49 assets更新/70再利用、consumer-auth binding、private/public R2、API health200、Auth health200/session nullをfresh readback。local dist旧marker0、served index直下JS marker0。実認証・実AI・実メール・実機・全通信zero・全工程完了とは分離。`print-design-detail` local verifier blockerは継続。
## 2026-09-08 print-design verifier boundary

The print-design-detail action now routes its visible generate button through the existing Cloudflare provider-generation and persistence path; the prior local SVG/input preview path is no longer bound to that action. Local CF build and diff checks pass. The focused verifier still times out waiting for `lightchain-print-design-readback` with no provider request and no UI error, so this remains an unresolved local UI-flow blocker and not provider-success evidence.
## 2026-09-08 print-design detail deployment

The detail route now renders the existing rights-confirmation modal before the Cloudflare edit-image request. The generate button has a dedicated test id, and the local readback verifier passed the saved Canvas project, Cloudflare provider request, workspace-artifact persistence, and provider-history metadata assertions. Heavy Web version `f670ceee-fc62-43eb-9dbd-cdab8f8a3653` is deployed at 100%; fresh health and served-chunk marker checks passed. This remains fixture/route evidence, not real-user AI quality, private-R2 business completion, device, email/OAuth, or traffic-zero proof.
