# Heavy Chain Current State

Updated: 2026-06-22

This file is the project-owned source of truth entrypoint for Codex/Obsidian resume. Generated Obsidian pages are locators, not execution proof.

## Obsidian Context Fields

current_state: Production app is deployed and QA-passed on app commit 7c93b450b77ad47c1a46773d5c74a865336fd645; release doctor passed with no STOP on 2026-06-21. Workspace parity UI now includes /studio, /models, /patterns, /video, and /lab. The previous `pattern-structured-context-local-v1` slice makes `/patterns` selected preview and structured pattern inputs survive the full `/patterns` -> Canvas/Gallery/History -> `/generate` -> `design-gacha` -> generated local artifact loop. `selectedPatternPreview` now carries id, label, mode, repeatSignature, vectorSignature, and paletteSignature; motifPrompt, repeatStyle, garmentTarget, paletteNotes, vectorIntent, and referenceAssets are preserved in the handoff `generationIntent.href`, handoff `generationIntent` body, `/generate` design-gacha request body, generated artifact metadata, and generated nested `generationIntent`. `referenceAssets` may be an empty string without dropping `patternContext`. Parent proof is saved at `output/playwright/pattern-structured-context-local-20260622/manifest.json`; it confirms empty-reference design-gacha request/provenance retention, non-empty `/patterns` handoff retention through the cross-workspace smoke, no Storage/REST mutation, and Codex review `Findings: none`. The previous `model-library-local-v1` slice adds `/models` as a Lightchain-style Model Library workspace: users can choose Clean EC 20s, Street LOOK 30s, or Premium AD 40s model candidates with `aria-pressed`, sync face / pose / body type / skin tone / age group / usage / product description into intake fields, see a deterministic SVG preview with `data-model-library="model-library-local-v1"` and `selected-model-candidate`, save `selectedModelCandidate` plus normalized `modelMatrixBodyTypes` / `modelMatrixAgeGroups` / `modelMatrixSkinTone` / `modelMatrixHairStyle` into local artifact metadata, workflow inputs/plan, top-level metadata, and Canvas image object parameters, then continue through Gallery detail / History / `/generate` with `sourceWorkspace=models`. The generated href now carries model-matrix query params, so Street LOOK 30s hydrates `/generate` to `bodyTypes=regular`, `ageGroups=30s`, `skinTone=medium`, and `hairStyle=medium` before invoking `model-matrix`. Parent proof is saved at `output/playwright/model-library-local-20260622/manifest.json`; it confirms `/models` desktop selection, Canvas handoff, Gallery detail, History timeline, `/generate` source banner, mocked `model-matrix` generation to `標準 × 30代`, no tracked REST mutation, exactly one mocked `/functions/v1/model-matrix` request, and no console/page errors. The targeted smoke now also confirms `/models` artifact href -> `/generate` -> `model-matrix` request body, generated local artifact provenance, and generated `generationIntent` retention of `bodyTypes=regular`, `ageGroups=30s`, `skinTone=medium`, and `hairStyle=medium`. The previous `generation-source-provenance-local-v1` slice lets sourced `/generate` runs save generated local artifacts with allowlist-hydrated source provenance: `sourceWorkspace`, `workflowVersion`, `sourceLabel`, `sourceResumePath`, `sourceMode`, and nested `generationIntent`. Gallery detail and History timeline expose an internal return link back to the originating workspace. Parent proof is saved at `output/playwright/generation-source-provenance-local-20260622/manifest.json`; it confirms source banner hydration, generated local artifact metadata, Gallery/History source links, exactly one mocked `/functions/v1/generate-image` request, no Storage/REST mutation, and no console/page errors. The previous `generation-context-readback-local-v1` slice adds source context from `/studio`, `/patterns`, `/video`, and `/lab` into each `generationIntent.href` and object: `sourceWorkspace`, `workflowVersion`, `sourceLabel`, `sourceResumePath`, and `sourceMode`; `/patterns` uses the current preview slice workflowVersion `pattern-preview-local-v1`. `/generate` now allowlist-hydrates that query context, shows a workspace resume banner, preserves existing `feature` / `prompt` / `ratio` prefill, and exposes an internal-only return link to the source workspace. Parent proof is saved at `output/playwright/generation-context-readback-local-20260622/manifest.json`; it confirms Studio, Lab, Video, and mobile Patterns resume banners, internal return links, Video `4:5` ratio prefill via the selected Portrait control, artifact-to-href readback via the workspace smoke, no Supabase Functions/Storage/tracked REST mutation, and no console/page errors. The previous `lab-evaluation-local-v1` slice extends `/lab` from structured intake to a Lightchain-style local evaluation selector. The previous `video-storyboard-local-v1` slice extends `/video` from structured intake to a Lightchain-style storyboard selector. The previous `studio-selection-local-v1` slice extends `/studio` from structured intake to a Lightchain-style local setup selector, and the previous `pattern-preview-local-v1` slice extends `/patterns` from brief handoff to local visual preview. AI fitting parity uses the existing `model-matrix` Edge Function as the durable persistence owner in local mock proof. Marketing parity routes persistence through the new `marketing-workspace-artifact` Edge Function contract in local mock proof. Deploy/RLS migration and production readback were intentionally not performed in this slice.
latest_slice: `source-context-summary-local-v1` makes Gallery detail and History timeline show user-readable `生成条件` from saved source metadata. `/models` outputs show `Street LOOK 30s` plus `regular / 30s / medium / medium`; `/patterns` outputs show `Bandana Grid`, `Smoke chain motif`, and `参照素材: なし`. The updated `model-matrix` source now also keeps `sourceReadback` and `modelCandidateLabel` in `generation_jobs.input_params` and `generated_images.metadata`, so remote DB readback can show the same source summary after deploy. Parent proof is saved at `output/playwright/source-context-summary-local-20260622/manifest.json`; it confirms local artifact readback, mocked remote generated_images/job readback, `deno check`, targeted Playwright, and Codex review `Findings: none`.
next_action: After explicit deploy approval, deploy `marketing-workspace-artifact` and the updated `model-matrix` Edge Function, then run production readback for Marketing and Fitting DB/Storage/job provenance. After that, connect /models, /studio, /patterns, /video, and /lab beyond local sourced generation provenance into real generation/readback entrypoints for model library, graphic/pattern/vector generation, scene/studio composition, video rendering, and lab simulation. Keep future storage/auth/RLS/payment/external-data/deploy changes behind fresh proof and approval.
blocker: none for the requested launch-readiness closeout. Service-role key rotation was not performed by user instruction.
risk_gate: Billing, purchase, payment, or checkout still requires explicit human approval. Future storage/auth/RLS/deploy/external-data changes require fresh proof.
maturity_candidate: release_prep
proof_locator: output/release-prep/final-production-20260621/auth-qa/qa-auth-summary.json; output/playwright/pattern-structured-context-local-20260622/manifest.json; output/playwright/model-library-local-20260622/manifest.json; output/playwright/generation-source-provenance-local-20260622/manifest.json; output/playwright/generation-context-readback-local-20260622/manifest.json; output/playwright/lab-evaluation-local-20260622/manifest.json; output/playwright/video-storyboard-local-20260622/manifest.json; output/playwright/studio-selection-local-20260622/manifest.json; output/playwright/pattern-preview-local-20260622/manifest.json; output/playwright/pattern-workspace-local-20260622/manifest.json; output/playwright/workspace-studio-video-lab-20260621/manifest.json; output/playwright/workspace-local-handoff-20260621/manifest.json; output/playwright/workspace-real-intake-local-20260621/manifest.json; output/playwright/workspace-generation-seed-local-20260621/manifest.json; output/playwright/workflow-query-prefill-local-20260621/manifest.json; output/playwright/workflow-board-local-20260622/manifest.json; output/playwright/fitting-parity-20260621/manifest.json; output/playwright/marketing-parity-20260621/manifest.json; output/playwright/local-artifact-readback-20260621/manifest.json; output/playwright/marketing-best-effort-remote-save-20260621/manifest.json; output/playwright/marketing-edge-function-save-20260621/manifest.json; output/playwright/fitting-durable-persistence-20260621/manifest.json
latest_proof_locator: output/playwright/source-context-summary-local-20260622/manifest.json
public_url_qa_locator: output/release-prep/final-production-20260621/public-url-qa/qa-summary.json
prod_db_readback_locator: output/playwright/prod-db-readback.json
cleanup_locator: output/release-prep/final-production-20260621/auth-qa/cleanup.json
env_locator: output/release-prep/final-production-20260621/release-doctor-after-deploy.txt
decision_locator: docs
runbook_locator: README.md
