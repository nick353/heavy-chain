# Print input fidelity — local candidate

## Latest fresh real-page acceptance — 2026-09-08

Fresh loopback browser run `49fe6c4f-d929-48e4-b454-13f8fbe52d6b` passed the final report's seven acceptance checks: two explicit reloads with one inference, two final-save attempts and one canonical reuse; six ordered layers/transforms/manual plane/output scale survived reload; pending input was acknowledged only after history save; final PNG was `1440x1800` with `0` protected RGBA mismatches and `154234` edited pixels; one restored history result contained seven source roles and the real page displayed one result. Server readback was mounts `3`, provider posts `1`, inferences `1`, final-save posts `2`, reuse posts `1`, receipt reads `1`, save reads `4`, media reads `1`; external fulfilled requests `0`, page errors `0`.

The probe-only server now pre-optimizes the actual TSX entry, keeps the native action disabled until the real workbench and six persisted inputs are ready, and surfaces initialization/React failures. Evidence: `work/heavy-print-input-probe-evidence.md` and final JSON under `work/.heavy-print-input-probe-fresh-flkMVe/`. This is loopback synthetic-fixture evidence only; production auth, Workers AI, private R2, real provider quality, device acceptance, and full migration completion remain unaccepted. Earlier unresolved browser runs remain historical and were not replayed.

## Latest local Gallery/history candidate — 2026-09-06

Deployment update13:06UTC: CF-auth build/typecheck/dry-run succeeded; Heavy Web version `16831a6c-c5c3-456b-a372-f7900c512a5b`100% readback. Served Gallery and print JS SHA match candidate, health200/cloudflare/session200null. No large R2 uploads. This supersedes the build/deploy-pending statements below only; real authenticated production/provider-save/AI quality remain unaccepted. Browser evidence is also persisted at ../../work/print-history-browser-scope-20260906.json.

Browser evidence 13:03 UTC: independent local port4178, run `a458ecf7-40a9-4413-8971-8bfa367023e4`, actual IndexedDB/PNG decode and real React page passed 10 checks both initially and after one signed full reload (no reseeding). Same-brand Alice/Bob isolation, different API origin, immediate old-result hiding on switch/logout and Alice-only restoration passed. Final screenshot showed two red Alice result cards. The fixture first lacked the workspace context and then supplied an incomplete exact-only result; corrected fixture uses the existing required exact+fabric pair, without changing production grouping. Companion tab1980913095/session `session_e38c1795-7067-4f46-aac3-d32b45397b23` closed with lease release, no foreign/unknown cleanup effects; new local server stopped. Storage-failure injection remains helper-only; modal/racing restore/full provider-save are not proven by these checks. CF-auth Web build has been started, not yet accepted/deployed. Old unknown operation remains unreplayed.

- GallerySelector now sends print-design purpose, generation-history has-job and favorites filters before API pagination. Focused client checks previously passed 2/2.
- PrintResultHistory v2 partitions metadata and asset references by API origin/user/brand. Legacy brand-only records are not adopted or deleted. Blob URLs persist as bytes; same-tab writes serialize and provider final save retains preceding results.
- React hides prior-scope results immediately and fences restoration/persistence against auth/brand changes. A failed restore permits fresh previews but does not enable automatic persistence. Storage denial, malformed JSON and non-array metadata reject rather than being overwritten as empty history.
- Latest focused helper/source tests passed 9/9 and tsc -b exited 0. Helper fixtures use synthetic storage; real browser IndexedDB, React account switching, full reload and new Web build/deployment remain pending. This is not real AI or production workflow evidence.
- Read-only Companion reconciliation at 12:41 UTC found old tab 1980913040 absent. Its transaction remains unknown_effect/orphaned_reconciliation_ttl; no old action was replayed and final save is not accepted. A future browser check must use an independent synthetic run/port. Old probe server and checkpoint were not reset.

The earlier brand-only-store and active-old-tab statements below are historical; the current candidate and unresolved receipt boundary above supersede them.

Updated: 2026-09-06 19:38 JST. Implementation and partial browser verification; final-save/full-history recovery and real AI quality remain unaccepted. No production changes in this slice.

## Implemented

- The existing ordered placement renderer includes all six selected designs, rotation, reflection, opacity and overlap in one precomposed input. The original full photograph is the second input; the protected-edit guide is third. No selected artwork is silently dropped to fit the reference limit. This is an AI input, not a generated result.
- Cutout coordinates map back into the full source photograph and the chosen 720×900 or 1440×1800 final frame. The selected frame can resample the source. References remain bounded to512px; this is not native AI upscaling or a guarantee about text/motif quality.
- The visible artwork footprint defines editable pixels; existing final compositing restores pixels outside that footprint. Actual print opacity is already in the source and is not applied twice.
- Existing print-input IndexedDB storage now has an origin/user/brand-scoped metadata envelope, exact Blob write readback, atomic revision replacement and stale-context checks. It preserves six-layer order/transforms, coverage, scale, confirmation and manual printable/occluder assets. Legacy brand-only data is not implicitly adopted by Cloudflare accounts. This does not prove account isolation for the separate, still brand-only PrintResultHistory store.
- The Cloudflare screen exposes all six inputs and existing placement/mask/output controls. An automatic ready mask allows editing but no longer claims explicit user review. The older rights-opening button still says `権限がありません`; source inspection confirms it opens rights confirmation, not an authorization failure. That misleading wording remains to fix after the current browser reconciliation boundary.

## Verified evidence

- Printing foundation244/244 passed before the final label-only change. Current input/runtime/geometry/source-wiring tests17/17 and `tsc -b` passed after that change. The latest Cloudflare-auth candidate build also passed on September 6 (`VITE_CLOUDFLARE_AUTH_ENABLED=true node cloudflare/heavy-web/build.mjs`, exit0; Vite2627 modules,16.10s). Output is local `.build/site` only; no asset upload, deployment or production auth cutover was performed. This does not resolve the pending browser verification below.
- Original native fixture `b62b418a-b8c6-4fe9-aacd-f659d5fb9881` proved12 native checks. Its in-memory server later stopped; its pending request `ea373cd2-5381-40a6-834d-48469d2fdf63` was not resubmitted and its completion is not claimed.
- New explicit synthetic fixture `f35a5c77-3173-4d38-afc6-a6011ddad2d9` uses a disk-checkpointed loopback transport. Actual React page, PNG/Canvas, input IndexedDB, provider preparation, UUID selection and final-save client execute; authentication, model response, HTTP persistence and signed-media delivery are fixtures.
- Native13 checks pass, including each of six visible artwork contributions, original-photo preservation, 720/1440 geometry, exact/fabric rendering, three bounded references and foreign-user input exclusion.
- Initial report adds6 checks: actual page submits the checked six-design composition, save failure retains pending UUID and bytes, editor layout is unchanged, and the initial report is not overwritten. One explicit full reload completed; mounts1→2, provider POST1/inference1/save POST1/save GET2/canonical saves0. Request `1a839c96-dd9a-4af1-a4d7-8cc360bf48b0` remains pending.
- Reports and synthetic transport checkpoint: coordinator `work/heavy-image-ai-20260906/f35a5c77-3173-4d38-afc6-a6011ddad2d9-print-{native,initial}-*.json` and `...-print-state.json`. Disk persistence is implemented. A separate new-run test now verifies restoration across two real child-process exits/restarts (see below); this active browser run was not restarted or completed.

## Current exact boundary

Opening the rights dialog after reload timed out during physical activation. Companion recorded `visual_input_activation_timeout` / `unknown_effect`, capsule `capsule_efc824aa-0e47-4f54-8b0a-43224cc8e1cd`, transaction key `heavy-print-recover-rights-open-20260906-2`. Same-tab readback shows no dialog; backend counters remain unchanged. Do not replay that operation or switch browser surfaces. Exact session `session_9b0888a2-47a0-48da-9686-0d45c6aaadf9`, tab1980913040 is retained for reconciliation. No new provider request was made during this failed dialog operation.

Next: resolve this exact browser reconciliation, then recover the existing request through receipt GET, complete deterministic final-save/readback/history/ack and a second real full reload. Acceptance remains one inference, two save attempts, one canonical result/reuse, all layout retained, final1440×1800 with zero outside-footprint RGBA differences and one restored history result. Never substitute this fixture for production/private-R2/real-model quality evidence.

Other retained gaps: Cloudflare Gallery filters purpose after its100-row limit; PrintResultHistory account scope; optional occluder pixel behavior; real print/mask/fitting quality; production authentication, CPU/cost and all eleven migration stages. Adaptive Orchestration is no longer used, per the user's latest instruction; continue directly using the existing project plan.

## Independent Gallery API follow-up — September 6

`core.ts` now accepts `asset_purpose=print-design` and `has_job=true|false`, combining those filters with owner/brand/favorite and intermediate exclusion before SQL pagination. Equal timestamps use `id DESC` for deterministic page boundaries. The API-package client exposes these options. Real SQLite tests seed120 newer unrelated images, older matching artwork, foreign-user/brand rows, intermediate results and malformed metadata; both filtered pages and generation-history filtering pass. API suite79/79 passed before the final client-only addition; latest targeted client/Gallery5/5 and final API typecheck pass. No real AI calls, migration or deployment.

The active frontend was deliberately unchanged while its exact browser operation remains unresolved. `src/lib/cloudflareApi.ts` and `GallerySelector.tsx` still need to send these filters; the current UI therefore still has the100-row truncation issue. Separately, inspection confirmed history keys/references depend only on brand/result and page effects only on brand, so PrintResultHistory user isolation is still unimplemented. Fix those existing paths after the retained browser operation is reconciled; do not claim this API-only slice completes either UI issue.

## Fixture restart evidence and exact browser recheck — September6

- At10:49:39Z signed transaction status still returned `unknown_effect`, `visual_input_activation_timeout`, `safe_fresh_retry_allowed=false`, `continuation_allowed=false` for the exact retained capsule above. Same-tab visual/semantic readback at10:49:57Z showed mount2, six inputs and no rights dialog or history result. No mutation was resent. The available pre-dispatch repair is explicitly limited to visual **inspection** timeouts, not this physical-input activation timeout, so it was not used.
- `node --test scripts/verify-print-probe-restart.test.mjs` passed1/1 (3.725s). It starts isolated loopback HTTP child processes with a new synthetic run in a unique temporary directory, writes a receipt and failed save, terminates/restarts, reads the same receipt, simulates a committed save with lost response, terminates/restarts again, reads that save by GET and verifies canonical reuse. Final state/disk equality: serverRestores2/providerPosts1/inferences1/finalSavePosts2/reusePosts1/receiptReads1/saveReads2/request1/save1. All test processes and its temporary synthetic files are cleaned up. Existing browser/process/run data are untouched.
- This strengthens only the fixture's disk-recovery evidence. It does not satisfy browser final-history/reload, private production R2, authenticated production or real AI quality acceptance. Full eleven-stage Goal remains active, with no Adaptive Orchestration.
