1. Preflight-only environment and production gate
   Goal: In `/Users/nichikatanaka/Desktop/アパレル１`, fresh-read `STATE.md`, `package.json`, `docs/lightchain-full-parity-audit-2026-06-22.md`, `docs/lightchain-parity-user-workflows-2026-06-21.md`, and latest `output/playwright/*` proof. Verify current env with `npm run env:check`, static Supabase gate with `npm run supabase:verify:static`, and security/edge gates. Do not apply migrations, deploy Web/Edge bundles, mutate DB/RLS/storage/auth, or run external writes from this child. If env/secrets or deploy approval are missing, record the exact blocker and minimum resume trigger. Completion requires command outputs summarized, artifact paths, and no secret values in output.

2. Bounded implementation gap closeout
   Goal: Audit and implement the smallest bounded local code changes needed for the current Lightchain parity gap set, using `docs/lightchain-full-parity-audit-2026-06-22.md` unresolved gaps as the scope. Prioritize one coherent slice across `/patterns`, `/video`, `/lab`, Canvas multi-step edit context, Gallery share/readback, Jobs durable step display, or source-context generation; do not attempt an unbounded full clone of Lightchain. Respect existing React/Vite/Supabase patterns and avoid broad refactors. Do not deploy or mutate external services. Completion requires code changes only if gaps exist, targeted tests, and a parent summary listing changed files, behavior, risk, and remaining product gaps.

3. Supabase readback and cleanup proof
   Goal: Prove that Lightchain-compatible generation metadata and durable task steps line up across jobs, images, storage, usage, runs, and `lightchain_task_steps`. Use existing collectors/verifiers such as `workspace:collect-readback` and `verify:workspace-readback` with `--expect-lightchain-task-codes`. Production-equivalent readback may run only after current parent approval for any required deploy/DB/RLS/storage/auth mutation is explicit and env is present. If production-equivalent readback cannot run, mark this child blocked with an exact blocker artifact; local fixture/readback may be verified only as baseline evidence, never as a substitute for production DB/Storage readback. Completion requires readback JSON, cleanup JSON, expected task codes, and zero remaining temporary users/storage rows for any live smoke data created by this run.

4. Real browser UI verification
   Goal: Run the app and verify the user-facing workflows with Playwright CLI, not static review only. Cover Dashboard Lightchain hub, `/studio`, `/patterns`, `/video`, `/lab`, `/generate` with Lightchain banner/request body, Canvas edit actions, Gallery detail/share behavior, History resume, Jobs step-state display, and responsive/mobile where relevant. Without explicit parent approval for live production writes, keep this child to mock/local proof only and do not create live Edge/API generation, DB/Storage/usage/run/share-link records. Production closeout requires cross-session or re-login-equivalent readback; if that cannot run, write an exact blocker artifact and mark production closeout incomplete. Include progress, success, stalled/failed, retry, and edit-handoff states. Capture screenshots/state/console/network or equivalent artifacts under `output/playwright/`. Completion requires nonblank UI proof, no relevant console/page errors, exact blocker artifacts for any failed stage, and cleanup/no residual process proof.

5. Release docs, STATE, and final parent audit
   Goal: Consolidate all child outputs into source-of-truth docs. Update `STATE.md`, relevant release/lightchain docs, proof locators, blockers, and next action. Run final static gates (`typecheck`, `lint`, `build` with safe env, `security:audit`, `supabase:verify:static`, `smoke:edge`, targeted Edge Function `deno check`, `git diff --check`) and review dirty changes. Treat Goal 1 as preflight and Goal 5 as final rerun/diff/readiness audit, not a deploy lane. Completion requires a concise final readiness statement: what is production-ready, what is local-proof-only, what remains blocked, exact proof paths, and whether Heavy Chain can now be used seriously as a Light Chain upper-compatible app.

Global stop boundary for every child: billing, purchase, payment, checkout, OTP, security code, CAPTCHA, identity verification, missing secrets, or any unapproved production deploy/DB/RLS/storage/auth mutation. If hit, write exact blocker evidence and finish with `blocked` or `failed`, not completed.
